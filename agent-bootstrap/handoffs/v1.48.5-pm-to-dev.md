# Handoff v1.48.5 — PM → AGENTE_PROGRAMADOR

## Resumen ejecutivo

Cambio quirúrgico en labit para que el barcode de las etiquetas codifique
**`{protocol_number}^{material_abbreviation}`** en lugar de solo `protocol_number`.

Esto **destraba v1.49.0** en LISCOM (que necesita saber el material para responder al equipo
con las determinaciones correctas).

**Estimación:** 1-2 horas. Es chico.

---

## Decisiones registradas

| # | Pregunta | Decisión |
|---|---|---|
| 1 | ¿Tema del barcode? | **Opción B**: cambiar formato en labit. Inserta esta versión v1.48.5 en la cadena, antes de v1.49.0 (liscom). |
| 2 | ¿Separator? | **`^`** (caret). Compatible con CODE_128, no choca con `|`, es estándar HL7 de componentes. |
| 3 | ¿Qué fields del barcode? | `{protocol_number}^{material_abbreviation}`. La `material_abbreviation` ya existe como accessor en `Test`. |
| 4 | ¿Etiquetas viejas en circulación? | **No reimprimir.** Liscom v1.49.0 acepta ambos formatos (con y sin `^`). |
| 5 | ¿Lab clínico, muestras, vet? | Lab clínico: una etiqueta por material (perfecto). Muestras: una etiqueta usando primer material (Opción 3.A en el prompt). Vet: verificar si genera etiquetas. |

---

## Tensiones identificadas (anotar al ejecutar, no resolver)

1. **JS de Zebra (`public/js/zebra-label-print.js`)**: si arma el ZPL leyendo `protocol_number`
   directo del JSON de `labelData()` (no del nuevo `barcode_content`), las impresoras Zebra
   imprimirán el formato VIEJO incluso después de este deploy. **Verificar y reportar al PM**:
   - Si confirma el problema: hotfix v1.48.5.1 agregando `barcode_content` al JSON y consumiéndolo
     en el JS.
   - Si está OK porque ya regenera el contenido del lado servidor: documentar.
2. **Muestras (`SampleController`)**: hoy una sola etiqueta concatena materiales. Solo se cambia el
   contenido del barcode (toma el primer material). UX igual. Cuando entre algún equipo HL7
   conectado a muestras, replanificar como v1.48.6.
3. **Veterinaria**: revisar `VetAdmissionController`. Si genera etiquetas, aplicar mismo patrón.

---

## Archivos a tocar

| Archivo | Cambio |
|---|---|
| `app/Services/BarcodeFormatService.php` | **CREAR** |
| `app/Http/Controllers/LabAdmissionController.php` | Líneas ~936-945 (método `printLabel`) |
| `app/Http/Controllers/SampleController.php` | Líneas ~1063-1064 (método `printLabel`) |
| `app/Http/Controllers/VetAdmissionController.php` | **VERIFICAR** si genera barcodes; aplicar si corresponde |
| `tests/Feature/BarcodeFormatTest.php` | **CREAR** (5 tests) |
| `CHANGELOG.md` | Entry v1.48.5 |
| `ROADMAP.md` | Mover v1.48.5 a Completadas |

---

## DoD resumida

- [ ] `BarcodeFormatService::forLabel(string, ?string): string` creado.
- [ ] `LabAdmissionController::printLabel` usa el servicio. Cada etiqueta lleva su material.
- [ ] `SampleController::printLabel` usa el servicio (Opción 3.A).
- [ ] `VetAdmissionController` verificado y modificado si corresponde.
- [ ] 5 tests Feature pasan: `php artisan test --filter=BarcodeFormatTest`.
- [ ] CHANGELOG y ROADMAP actualizados.
- [ ] Commit: `v1.48.5: extender formato de barcode a {protocol_number}^{material_abbreviation}`.

---

## Próximo paso después de v1.48.5

Pasar a **v1.49.0** (LISCOM) — `pendientes/v1.49.0-liscom-mapeo-codigos-respuesta-scan.md`.
**OJO: ese se ejecuta en `c:\wamp64\www\interfases`**, NO en este repo.

Para v1.49.0 hay un Designer recomendado para la UI de mapeos (lista por equipo, alta inline).
Coordinar con AGENTE_DESIGNER de la instancia de liscom.
