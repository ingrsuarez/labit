# Handoff PM → Dev — v1.101.1 (Resultados vet sin recarga)

**Sesión PM:** 2026-05-17  
**Versión:** v1.101.1 — Veterinario: validar / desvalidar / quitar sin recarga (Livewire)  
**Prompt de tarea:** `agent-bootstrap/prompts/pendientes/v1.101.1-vet-admission-results-livewire-sin-recarga.md`  
**Depende de:** v1.101.0  
**Diseño:** no aplica

---

## Contexto

Paridad con v1.101.0: el operador valida protocolos veterinarios con la misma fricción de scroll por POST+redirect+fragmento `#vet-admission-results`.

---

## Decisiones PM

| Decisión | Valor |
|----------|--------|
| Ejecutar después de | **v1.101.0** (reutilizar toast, convenciones) |
| Alcance | validate / unvalidate / remove en tabla Resultados |
| Fuera de scope | `loadResults`, `validateAll` |
| Orden de filas | `VetAdmission::getVetTestsOrderedForDisplay()` — no reimplementar |
| Jerarquía | `VetAdmissionTestDisplayOrder` |
| Diferencia vs clínico | Campo `status` en `VetAdmissionTest`; ajuste `total_price` al quitar |
| DELETE remove | Rutas HTTP siguen DELETE; Livewire llama al servicio, no al wire DELETE |

---

## Archivos clave

1. `agent-bootstrap/prompts/pendientes/v1.101.1-vet-admission-results-livewire-sin-recarga.md`
2. Implementación v1.101.0 (toast, Livewire lab) como plantilla
3. `resources/views/vet/admissions/show.blade.php`
4. `app/Http/Controllers/VetAdmissionController.php`
5. `tests/Feature/RemoveLeafAdmissionDeterminationTest.php` (casos vet)

---

## Advertencias

1. No olvidar `status` al validar (`validated`) y al desvalidar (`completed` / `pending`).
2. Tests de `total_price` al eliminar hoja con precio > 0.
3. Recepcion-lab: misma restricción que en controlador (`status !== pending` no elimina).
4. Eliminar `vetSubmitAction` / `vetDeleteAction` solo si no quedan referencias en la vista.

---

## Siguiente sugerida

- **v1.101.2** — Guardar resultados (lab + vet) sin recarga completa.
