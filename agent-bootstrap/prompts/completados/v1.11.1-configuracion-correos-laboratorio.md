# v1.11.1 — Configuración de correos del laboratorio

**VERSIÓN:** v1.11.1  
**SLUG:** configuracion-correos-laboratorio  
**DEPENDENCIAS:** ninguna técnica  
**ESTIMACIÓN:** 2h  
**PRIORIDAD:** alta

---

## Descripción

Agregar una pantalla de configuración de emails del laboratorio accesible desde
`/lab/configuracion`. Se definen dos remitentes:

1. **Resultados**: dirección desde donde se envían los informes PDF a clientes
2. **Notificaciones**: dirección desde donde salen avisos del laboratorio (feriados, etc.)

Cada remitente tiene 4 campos: email, nombre del remitente, asunto por defecto y firma HTML.

Además, se implementa el envío real de resultados por email (reemplazando el TODO
actual en `SampleController::sendEmail`).

---

## PASO 0 — Contexto previo a leer

Leer sin ejecutar nada:

- `app/Models/PayrollSetting.php` — patrón clave-valor a replicar
- `app/Http/Controllers/LabSectionController.php` — método `configuracion()` (líneas 85-125)
- `app/Http/Controllers/SampleController.php` — método `sendEmail()` (líneas 767-783, es un TODO)
- `app/Http/Controllers/QuoteController.php` — método `sendEmail()` (líneas 252-268, referencia de envío real)
- `app/Mail/QuoteMail.php` — Mailable de referencia con PDF adjunto
- `resources/views/emails/quote.blade.php` — template de email de referencia
- `resources/views/lab/section.blade.php` — vista de la página de configuración (cards)
- `config/mail.php` — configuración actual de mail
- `routes/web.php` — rutas del lab

---

## PASO 1 — Migración: crear tabla `lab_settings`

```php
Schema::create('lab_settings', function (Blueprint $table) {
    $table->id();
    $table->string('key')->unique();
    $table->text('value')->nullable();
    $table->timestamps();
});
```

Ejecutar: `php artisan migrate`

---

## PASO 2 — Modelo LabSetting

Crear `app/Models/LabSetting.php` siguiendo el patrón de `PayrollSetting`:

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class LabSetting extends Model
{
    protected $fillable = ['key', 'value'];

    public static function get(string $key, $default = null): ?string
    {
        return static::where('key', $key)->value('value') ?? $default;
    }

    public static function set(string $key, ?string $value): void
    {
        static::updateOrCreate(['key' => $key], ['value' => $value]);
    }
}
```

### Claves que se usarán

| Clave | Tipo | Ejemplo |
|---|---|---|
| `results_email` | string | `resultados@ipac.com.ar` |
| `results_from_name` | string | `IPAC Laboratorio - Resultados` |
| `results_default_subject` | string | `Informe de Resultados - Protocolo {protocol}` |
| `results_signature` | text/html | `<p>Atentamente,<br>IPAC Laboratorio</p>` |
| `notifications_email` | string | `laboratorio@ipac.com.ar` |
| `notifications_from_name` | string | `IPAC Laboratorio` |
| `notifications_default_subject` | string | `Aviso del Laboratorio` |
| `notifications_signature` | text/html | `<p>IPAC Laboratorio<br>Leguizamón 356...</p>` |

---

## PASO 3 — Rutas

En `routes/web.php`, dentro del grupo `lab` con middleware de permisos:

```php
Route::get('/lab/configuracion/correos', [App\Http\Controllers\LabEmailSettingsController::class, 'edit'])
    ->name('lab.email-settings.edit');
Route::put('/lab/configuracion/correos', [App\Http\Controllers\LabEmailSettingsController::class, 'update'])
    ->name('lab.email-settings.update');
```

---

## PASO 4 — Controlador LabEmailSettingsController

Crear `app/Http/Controllers/LabEmailSettingsController.php`:

```php
<?php

namespace App\Http\Controllers;

use App\Models\LabSetting;
use Illuminate\Http\Request;

class LabEmailSettingsController extends Controller
{
    private array $settingKeys = [
        'results_email',
        'results_from_name',
        'results_default_subject',
        'results_signature',
        'notifications_email',
        'notifications_from_name',
        'notifications_default_subject',
        'notifications_signature',
    ];

    public function edit()
    {
        $settings = [];
        foreach ($this->settingKeys as $key) {
            $settings[$key] = LabSetting::get($key, '');
        }

        return view('lab.email-settings', compact('settings'));
    }

    public function update(Request $request)
    {
        $request->validate([
            'results_email' => 'required|email',
            'results_from_name' => 'required|string|max:255',
            'results_default_subject' => 'required|string|max:255',
            'results_signature' => 'nullable|string',
            'notifications_email' => 'required|email',
            'notifications_from_name' => 'required|string|max:255',
            'notifications_default_subject' => 'required|string|max:255',
            'notifications_signature' => 'nullable|string',
        ]);

        foreach ($this->settingKeys as $key) {
            if ($request->has($key)) {
                LabSetting::set($key, $request->input($key));
            }
        }

        return back()->with('success', 'Configuración de correos actualizada correctamente.');
    }
}
```

---

## PASO 5 — Vista de configuración de correos

Crear `resources/views/lab/email-settings.blade.php`:

### Estructura de la vista

- Layout: `x-lab-layout` (consistente con el resto del lab)
- Header: "Configuración de Correos" con breadcrumb a `/lab/configuracion`
- Formulario con 2 secciones separadas visualmente:

**Sección 1: Correo de Resultados**
- Email remitente (input email)
- Nombre del remitente (input text)
- Asunto por defecto (input text, con hint: "Usá {protocol} para incluir el nro de protocolo")
- Firma HTML (textarea grande con preview en tiempo real usando Alpine.js)

**Sección 2: Correo de Notificaciones**
- Los mismos 4 campos

**Firma HTML — Editor:**
Usar un `<textarea>` con preview del HTML renderizado al lado (split view) usando Alpine.js:

```html
<div x-data="{ html: '{{ old('results_signature', $settings['results_signature']) }}' }">
    <textarea x-model="html" name="results_signature" rows="8"
              class="w-full font-mono text-sm ..."></textarea>
    <div class="mt-2 border rounded-lg p-4 bg-white">
        <p class="text-xs text-gray-400 mb-2">Vista previa:</p>
        <div x-html="html"></div>
    </div>
</div>
```

**Botón Guardar** al final del formulario.

### Estilo

Usar el mismo patrón visual que `payroll.settings` o la vista de perfil:
cards separadas, labels claros, botón teal.

---

## PASO 6 — Agregar card en la página de configuración

En `app/Http/Controllers/LabSectionController.php`, método `configuracion()`,
agregar un nuevo item al array `$section['items']`:

```php
[
    'name' => 'Correos',
    'description' => 'Configuración de correos de resultados y notificaciones',
    'route' => route('lab.email-settings.edit'),
    'icon' => 'M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z',
],
```

Esto agrega la card "Correos" con ícono de sobre en la grilla de `/lab/configuracion`.

---

## PASO 7 — Implementar envío real de resultados

### 7.1 — Crear Mailable `SampleResultMail`

Crear `app/Mail/SampleResultMail.php`:

```php
<?php

namespace App\Mail;

use App\Models\Sample;
use App\Models\LabSetting;
use Illuminate\Bus\Queueable;
use Illuminate\Mail\Mailable;
use Illuminate\Mail\Mailables\Content;
use Illuminate\Mail\Mailables\Envelope;
use Illuminate\Queue\SerializesModels;
use PDF;

class SampleResultMail extends Mailable
{
    use Queueable, SerializesModels;

    public Sample $sample;
    public ?string $customMessage;
    public ?string $signature;

    public function __construct(Sample $sample, ?string $customMessage = null)
    {
        $this->sample = $sample;
        $this->customMessage = $customMessage;
        $this->signature = LabSetting::get('results_signature', '');
    }

    public function envelope(): Envelope
    {
        $subject = LabSetting::get('results_default_subject', 'Informe de Resultados');
        $subject = str_replace('{protocol}', $this->sample->protocol_number, $subject);

        return new Envelope(subject: $subject);
    }

    public function content(): Content
    {
        return new Content(view: 'emails.sample-result');
    }

    public function attachments(): array
    {
        $this->sample->load([
            'customer',
            'determinations.test.parentTest',
            'determinations.test.parentTests',
            'determinations.test.children',
            'determinations.test.childTests',
            'determinations.test.defaultReferenceCategory',
            'determinations.referenceCategory',
            'determinations.determinationValidator',
            'creator',
            'validator',
        ]);

        $pdf = PDF::loadView('sample.pdf-mpdf', ['sample' => $this->sample], [], [
            'margin_top' => 35,
            'margin_bottom' => 20,
            'margin_left' => 15,
            'margin_right' => 15,
        ]);

        // Usar el mismo nombre descriptivo de v1.9.0
        $filename = app(\App\Http\Controllers\SampleController::class)
            ->generatePdfFilename($this->sample);
        // Si generatePdfFilename es privado, duplicar la lógica aquí o hacerlo protected

        return [
            \Illuminate\Mail\Mailables\Attachment::fromData(
                fn () => $pdf->output(),
                $filename
            )->withMime('application/pdf'),
        ];
    }
}
```

**Nota sobre `generatePdfFilename`:** Si el método es `private` en `SampleController`,
cambiarlo a `public static` o duplicar la lógica en el Mailable. La lógica es:
`{sample_type}-{customer_name}-{date}.{protocol_number}.pdf` sanitizado.

### 7.2 — Crear template de email

Crear `resources/views/emails/sample-result.blade.php`:

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
</head>
<body style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p>Estimado/a cliente,</p>

    <p>Adjunto encontrará el informe de resultados del protocolo
       <strong>{{ $sample->protocol_number }}</strong>.</p>

    @if($customMessage)
        <p>{{ $customMessage }}</p>
    @endif

    <hr style="border: none; border-top: 1px solid #eee; margin: 20px 0;">

    @if($signature)
        {!! $signature !!}
    @else
        <p>Atentamente,<br>Laboratorio IPAC</p>
    @endif
</body>
</html>
```

### 7.3 — Modificar `SampleController::sendEmail`

Reemplazar el método TODO actual:

```php
public function sendEmail(Request $request, Sample $sample)
{
    $this->authorize('samples-reports.send');

    if (!$sample->isValidated()) {
        return back()->with('error', 'El protocolo debe estar validado para poder enviarlo.');
    }

    $validated = $request->validate([
        'email' => 'required|email',
        'message' => 'nullable|string',
    ]);

    $fromEmail = LabSetting::get('results_email', config('mail.from.address'));
    $fromName = LabSetting::get('results_from_name', config('mail.from.name'));

    Mail::mailer('smtp')
        ->to($validated['email'])
        ->send(
            (new SampleResultMail($sample, $validated['message'] ?? null))
                ->from($fromEmail, $fromName)
        );

    return back()->with('success', 'Protocolo enviado correctamente a ' . $validated['email']);
}
```

Agregar los imports necesarios al controller:
```php
use App\Mail\SampleResultMail;
use App\Models\LabSetting;
use Illuminate\Support\Facades\Mail;
```

---

## PASO 8 — Verificación funcional

### Test 1: Card en configuración
1. Ir a `/lab/configuracion`
2. Debe aparecer la card "Correos" con ícono de sobre
3. Click → abre `/lab/configuracion/correos`

### Test 2: Formulario de configuración
1. Ver los 8 campos divididos en 2 secciones
2. Completar los campos de resultados:
   - Email: `resultados@ipac.com.ar`
   - Nombre: `IPAC Laboratorio`
   - Asunto: `Informe de Resultados - Protocolo {protocol}`
   - Firma: `<p><strong>IPAC Laboratorio</strong><br>Leguizamón 356. TEL: 0299-6227547</p>`
3. Completar los campos de notificaciones
4. Guardar → mensaje de éxito
5. Recargar → los valores se mantienen

### Test 3: Preview de firma HTML
1. Escribir HTML en el textarea de firma
2. La preview debajo se actualiza en tiempo real
3. Probar con tags: `<strong>`, `<br>`, `<img>`, `<p>`

### Test 4: Envío real de resultados
1. Ir a un protocolo validado
2. Click en enviar por email
3. Ingresar un email de destino
4. Verificar que el email llega con:
   - Remitente = el configurado en settings
   - Asunto = el configurado, con `{protocol}` reemplazado
   - Cuerpo con el mensaje personalizado (si se escribió)
   - Firma HTML renderizada
   - PDF adjunto con nombre descriptivo

### Test 5: Fallback sin configuración
1. Borrar las settings de la BD
2. Enviar un email → debe usar los valores de `config('mail.from.*')` como fallback
3. No debe dar error

### Test 6: Validación
1. Intentar guardar con email inválido → error de validación
2. Intentar guardar sin nombre → error de validación

---

## PASO N — Commit y tag

```bash
VERSION="v1.11.1"
SLUG="configuracion-correos-laboratorio"

git add app/Models/LabSetting.php \
        app/Http/Controllers/LabEmailSettingsController.php \
        app/Http/Controllers/LabSectionController.php \
        app/Http/Controllers/SampleController.php \
        app/Mail/SampleResultMail.php \
        database/migrations/*create_lab_settings*.php \
        resources/views/lab/email-settings.blade.php \
        resources/views/emails/sample-result.blade.php \
        routes/web.php

git commit -m "feat(lab): configuración de correos y envío real de resultados

- Modelo LabSetting (clave-valor) para config del laboratorio
- Pantalla de config con 2 remitentes: resultados y notificaciones
- 4 campos por remitente: email, nombre, asunto, firma HTML
- Editor de firma con preview en tiempo real
- SampleResultMail: envío real de PDF con firma configurada
- Reemplaza TODO en SampleController::sendEmail"

git tag -a "${VERSION}" -m "Release v1.11.1: Configuración de correos del laboratorio"
```

---

## Instrucciones de producción

```bash
php artisan migrate
php artisan view:clear
```

Después de migrar, ir a `/lab/configuracion/correos` y completar las direcciones
de email antes de usar el envío de resultados.

---

## Verificación final

- [ ] Migración crea tabla `lab_settings`
- [ ] Card "Correos" aparece en `/lab/configuracion`
- [ ] Formulario muestra 2 secciones con 4 campos cada una
- [ ] Guardar persiste los valores correctamente
- [ ] Preview de firma HTML funciona en tiempo real
- [ ] Envío de resultados usa la config guardada (remitente, asunto, firma)
- [ ] PDF adjunto con nombre descriptivo
- [ ] Placeholder `{protocol}` se reemplaza en el asunto
- [ ] Fallback a `config('mail.from.*')` si no hay settings
- [ ] Validación de campos obligatorios funciona
- [ ] No hay errores de lint ni consola JS
