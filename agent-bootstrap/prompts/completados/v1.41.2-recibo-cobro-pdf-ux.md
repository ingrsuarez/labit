# v1.41.2 — Logo IPAC en PDF recibo de cobro + acceso PDF en listado

**VERSIÓN:** v1.41.2  
**SLUG:** recibo-cobro-pdf-ux  
**DEPENDENCIAS:** v1.41.0 (PDF recibo de cobro base)  
**ESTIMACIÓN:** corta  
**PRIORIDAD:** media  

---

## Descripción

1. Incluir el **logo IPAC** en la cabecera del PDF de recibo de cobro (`collection-receipts/pdf.blade.php`), alineado al patrón de otros PDFs del repo (`public/images/logo_ipac.png`, `file_exists` defensivo).
2. Agregar en el **listado** de recibos (`collection-receipts/index.blade.php`) un enlace/botón **PDF** por fila (misma autorización que ver/descargar PDF: usuario con acceso a recibos), coherente visualmente con el botón PDF de la vista show (rojo, compacto en columna acciones).

**Fuera de scope:** cambiar plantilla de email, otros módulos.

---

## PASO 1 — PDF

- En cabecera del PDF, mostrar logo si existe el archivo; debajo o al lado los datos de empresa como hoy.

## PASO 2 — Listado

- Columna acciones: además de «Ver», enlace «PDF» a `route('collection-receipts.pdf', $receipt)`.

## PASO 3 — Activo estático

- Asegurar `public/images/logo_ipac.png` en el repo si no existía (asset corporativo).

## PASO 4 — Tests

- Mantener verde `CollectionReceiptPdfTest` (o extender si aplica).

---

## PASO 5 — Commit y tag

```bash
VERSION="v1.41.2"
git add public/images/logo_ipac.png resources/views/collection-receipts/pdf.blade.php resources/views/collection-receipts/index.blade.php
git commit --no-verify -m "feat(collection-receipts): logo IPAC en PDF y PDF en listado (v1.41.2)"
git tag -a "${VERSION}" -m "Release ${VERSION}: UX PDF recibos de cobro"
```

Tras merge: mover este prompt a `completados/`, actualizar `ROADMAP.md` y `STATUS.md`.
