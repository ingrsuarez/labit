# v1.99.1 — Hotfix: NC desde factura electrónica — no colgar en autorización AFIP

**Estado:** completado en código (2026-05-14).

**Implementación resumida:**

- `config/afip.php`: `soap_connection_timeout_seconds`, `soap_read_timeout_seconds` (env `AFIP_SOAP_CONNECTION_TIMEOUT`, `AFIP_SOAP_READ_TIMEOUT`).
- `AfipService`: `connection_timeout` en WSAA/WSFE; `withAfipSoapReadTimeout()` para lectura SOAP; `getLastVoucher`, `FECAESolicitar` (FV/NC), `FEDummy`, padrón y `loginCms` acotados.
- `CreditNoteController@store`: transacción corta solo para crear NC + ítems; AFIP fuera de la transacción; actualización CAE/número en transacciones cortas; `app(AfipService::class)` para permitir doble de prueba.
- `credit-notes/create.blade.php`: `@submit` en el formulario (electrónico) en lugar de `@click` en el botón.
- Tests: `tests/Feature/CreditNoteElectronicStoreTest.php`.

El prompt de planificación original se archivó desde `pendientes/` al cerrar la tarea (si no figura en git, ver historial de la sesión PM).
