# v1.44.0 — Nomenclador veterinario (hub + listado filtrado)

**DEPENDENCIAS:** v1.43.0

## Objetivo
- En `/lab/veterinario`, ítem **Nomenclador veterinario**.
- Ruta bajo `lab.section`: listado solo determinaciones `categories` contiene `veterinario` y `whereDoesntHave('parentTests')`.
- Reutilizar `test/index.blade.php`; enlaces a `tests.reference-values.index` sin cambios.
- Tras crear/editar/eliminar desde este contexto, redirigir al nomenclador vet (`_context=vet_nomenclator`).

## Cierre
```bash
git commit -m "feat(lab): nomenclador veterinario en hub vet (v1.44.0)"
git tag -a v1.44.0 -m "v1.44.0 — Nomenclador veterinario"
```
