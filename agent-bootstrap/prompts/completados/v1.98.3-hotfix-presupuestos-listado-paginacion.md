# v1.98.3 ÔÇö Hotfix: listado de presupuestos (paginaci├│n, totales y orden)

**VERSI├ôN:** v1.98.3  
**SLUG:** hotfix-presupuestos-listado-paginacion  
**DEPENDENCIAS:** ninguna  
**ESTIMACI├ôN:** 1h ÔÇö 2h  
**PRIORIDAD:** alta

---

## Descripci├│n

En **Presupuestos** (`/quotes`, `QuoteController@index`, `resources/views/quote/index.blade.php`) el usuario reporta no poder ver presupuestos ÔÇ£anterioresÔÇØ ni con numeraci├│n mayor a la ├║ltima visible (ej. no aparece nada por encima de `PRES-2026-00032`), y en la captura **no hay controles de paginaci├│n** ni resumen de total.

El listado hoy:

- Filtra por `company_id` = `active_company_id()`.
- Ordena por `created_at` descendente.
- Pagina con `paginate(15)->withQueryString()`.
- Solo renderiza `{{ $quotes->links() }}` dentro de `@if($quotes->hasPages())`.

**Hip├│tesis a descartar en este orden:**

1. **Menos de 16 registros** para la empresa activa: `hasPages()` es falso ÔåÆ no hay links; el usuario interpreta huecos en `quote_number` como ÔÇ£faltan filasÔÇØ, pero pueden ser borrados u otros a├▒os. Hay que **mostrar siempre** un resumen tipo ÔÇ£Mostrando XÔÇôY de ZÔÇØ para alinear expectativa con datos.
2. **Hay m├ís de 15 registros** pero los links no se ven: revisar **CSS del layout** (`x-lab-layout`, contenedores `overflow-hidden`, z-index) comparando con otra vista que s├¡ muestre paginaci├│n (ej. `sales-invoices/index`).
3. **Orden por `created_at`**: un presupuesto con n├║mero alto y `created_at` antiguo puede quedar en p├ígina 2+; el usuario busca ÔÇ£por n├║meroÔÇØ. Valorar **orden secundario** o criterio de orden configurable (m├¡nimo: `orderByDesc('quote_number')` si es estable con el formato `PRES-AAAA-#####`, o documentar en UI que el orden es por fecha de creaci├│n).
4. **Datos:** verificar en BD `quotes` con el `company_id` activo: `count(*)`, `max(quote_number)`, existencia de filas con n├║mero > 32.

**Fuera de alcance:** redise├▒o del m├│dulo; cambiar numeraci├│n global (`Quote::generateQuoteNumber` sin `company_id` es deuda conocida pero **no** es causa directa de filas ocultas en el ├¡ndice).

---

## PASO 0 ÔÇö Contexto previo a leer

> Si ven├¡s del AGENTE_WORKFLOW.md, saltear este paso.

Leer sin ejecutar nada:

- `app/Http/Controllers/QuoteController.php` (m├®todo `index`)
- `resources/views/quote/index.blade.php`
- `app/Models/Quote.php` (`generateQuoteNumber` solo para contexto de huecos en numeraci├│n)
- `resources/views/components/lab-layout.blade.php` (u layout equivalente) ÔÇö buscar `overflow`, `min-h`, contenedor principal
- Una vista de referencia con paginaci├│n visible: `resources/views/sales-invoices/index.blade.php`

---

## PASO 1 ÔÇö Diagn├│stico reproducible

1. Con la misma empresa activa que el usuario, ejecutar (tinker o query) conteo de `quotes` por `company_id` y el `max(quote_number)` del a├▒o en curso.
2. Confirmar si `total > 15` en el paginador coincide con la expectativa del usuario.
3. Si `total <= 15`, documentar en comentario de PR / nota breve que la UX previa ocultaba el total y generaba la confusi├│n.

**Verificaci├│n:** queda anotado en el PR o en el mensaje de commit el resultado del conteo (sin pegar datos sensibles de clientes).

---

## PASO 2 ÔÇö Vista: resumen de resultados siempre visible

En `resources/views/quote/index.blade.php`, cuando `$quotes->count() > 0`:

- Mostrar una fila bajo la tabla o sobre la paginaci├│n con texto del estilo:  
  `Mostrando {{ $quotes->firstItem() }}ÔÇô{{ $quotes->lastItem() }} de {{ $quotes->total() }} presupuesto(s)`  
  (usar pluralizaci├│n idiom├ítica en espa├▒ol si el proyecto ya tiene patr├│n).

Mantener `{{ $quotes->links() }}` cuando `hasPages()` sea verdadero.

**Verificaci├│n:** con 1, 8 y 20+ presupuestos de prueba (factory o seeder local), el texto refleja `total` correcto.

---

## PASO 3 ÔÇö Paginaci├│n visible y coherente con el resto del lab

1. Si el diagn├│stico muestra `hasPages() === true` pero los links no aparecen en pantalla: corregir layout/CSS siguiendo el patr├│n de `sales-invoices/index` (contenedor de links con padding y borde superior).
2. Si los links usan estilos inconsistentes, alinear con el resto del m├│dulo (Tailwind; Laravel 11 ya usa vista `pagination::tailwind` por defecto ÔÇö no introducir Bootstrap).

**Verificaci├│n:** con m├ís de 15 ├¡tems, ÔÇ£SiguienteÔÇØ / n├║meros de p├ígina son clicables y conservan filtros `search` y `status` (`withQueryString()` ya est├í en el controlador).

---

## PASO 4 ÔÇö Orden del listado (solo si el PM/usuario confirma preferencia por n├║mero)

Si tras el resumen de totales el problema persiste por **orden por fecha**:

- Cambiar el `orderBy` del query en `QuoteController@index` a orden por n├║mero de presupuesto descendente **solo si** se valida que `quote_number` ordena lexicogr├íficamente bien con el formato fijo `PRES-YYYY-#####` (padding de 5 d├¡gitos).  
- Si no es seguro, dejar `created_at` y a├▒adir **solo** `orderByDesc('quote_number')` como secundario tras criterio acordado.

**Verificaci├│n:** el presupuesto con mayor sufijo num├®rico del a├▒o aparece primero si ese es el criterio elegido.

---

## PASO 5 ÔÇö Test automatizado (recomendado)

Agregar un test Feature m├¡nimo, por ejemplo `tests/Feature/QuoteIndexPaginationTest.php`:

- Crear usuario con empresa y **18** `Quote` con el mismo `company_id` (factory si existe, o `Quote::factory()` / modelo m├¡nimo).
- `get(route('quotes.index'))` ÔåÆ assert status 200, assert que el HTML incluye el texto de total o fragmento estable del paginador (evitar asserts fr├ígiles a todo el HTML de Tailwind).

**Verificaci├│n:** `php artisan test --filter=QuoteIndex` pasa.

---

## PASO 6 ÔÇö Commit y tag

```bash
VERSION="v1.98.3"
SLUG="hotfix-presupuestos-listado-paginacion"

git add app/Http/Controllers/QuoteController.php resources/views/quote/index.blade.php tests/Feature/QuoteIndexPaginationTest.php
git commit --no-verify -m "fix(quotes): listado presupuestos ÔÇö totales visibles y paginaci├│n

- Resumen \"Mostrando XÔÇôY de Z\" en ├¡ndice de presupuestos
- Ajuste de orden o layout seg├║n diagn├│stico (ver cuerpo del PR)
- Test Feature de paginaci├│n / conteo"

git tag -a "${VERSION}" -m "Release ${VERSION}: hotfix listado presupuestos"
```

(Ajustar lista de archivos al diff real.)

---

## Verificaci├│n final

- [ ] Con empresa de prueba, el total mostrado coincide con `quotes` en BD para ese `company_id`.
- [ ] Con >15 registros, la paginaci├│n es usable y preserva filtros GET.
- [ ] Sin regresiones en filtros `search` y `status`.
- [ ] Tests Feature nuevos o existentes en verde.

---

## Notas

- Los **huecos** en `PRES-2026-#####` son normales si hubo borrados o numeraci├│n global entre empresas; el resumen ÔÇ£de ZÔÇØ reduce malentendidos.
- Si el usuario necesita ver **otros a├▒os**, valorar filtro por a├▒o en una versi├│n posterior (fuera de este hotfix).
