# v1.63.0 — Percepciones de compra: catálogo, FC y saldos

**VERSIÓN:** v1.63.0  
**SLUG:** percepciones-compra  
**DEPENDENCIAS:** v3.4.0 (Libro Diario/Mayor — cuentas contables disponibles), v1.42.0 (Servicios de compra — patrón catálogo)  
**ESTIMACIÓN:** 1 día  
**PRIORIDAD:** alta — desbloquea v1.63.1 y v1.64.0; cierra el círculo "anticipos sufridos" pedido por el cliente

---

## Descripción

Implementar el módulo completo de **percepciones de compra**:

1. **Catálogo `PurchasePerception`** — tipos de percepciones por empresa (nombre, jurisdicción, alícuota, cuenta contable)
2. **Pivote `purchase_invoice_perceptions`** — líneas de percepción por FC con snapshots
3. **Bloque multi-línea Alpine en FC** — reemplaza el `input[type=number]` suelto de "Percepciones"
4. **Ajuste de `AccountingEntryService::fromPurchaseInvoice`** — cada percepción debita su cuenta snapshot (en lugar de ir toda a 5.1.02)
5. **Pantalla de saldos** `/purchase-perceptions/balances` — anticipos cargados vs. Libro Mayor

---

## PASO 0 — Contexto previo

> Si venís del AGENTE_WORKFLOW.md, saltear este paso.

**Documentación de diseño:**
- `docs/design/v1.63.0-percepciones-compra.md` — mockups y especificación de pantallas
- `assets/mockup-v1.63.0-percepciones-fc.png` — bloque de percepciones en FC
- `assets/mockup-v1.63.0-saldos-percepciones.png` — pantalla de saldos

**Documentación del proyecto:**
- `BLUEPRINT.md` — ver DD-009 (facturación masiva — mismo módulo compras)
- `STATUS.md` — estado actual

**Código de referencia (patrón análogo):**
- `app/Models/PurchaseServiceCategory.php` + `PurchaseService.php` (v1.42.0) → patrón catálogo
- `app/Http/Controllers/PurchaseServiceCategoryController.php` → patrón CRUD de catálogo
- `resources/views/purchase-invoices/create.blade.php` → sección de ítems multi-línea Alpine (copiar patrón)
- `app/Services/AccountingEntryService.php::fromPurchaseInvoice()` línea ~265 → método a modificar

---

## PASO 1 — Migración y modelo `PurchasePerception` (catálogo)

```bash
php artisan make:migration create_purchase_perceptions_table --create=purchase_perceptions
```

Esquema:

```php
Schema::create('purchase_perceptions', function (Blueprint $table) {
    $table->id();
    $table->foreignId('company_id')->constrained()->cascadeOnDelete();
    $table->foreignId('accounting_account_id')
          ->constrained('accounting_accounts')
          ->restrictOnDelete();
    $table->string('name');
    $table->string('jurisdiction')->nullable();
    $table->decimal('rate', 5, 2)->default(0);
    $table->boolean('is_active')->default(true);
    $table->unsignedSmallInteger('sort_order')->default(0);
    $table->timestamps();

    $table->index(['company_id', 'is_active']);
});
```

Crear `app/Models/PurchasePerception.php`:

```php
protected $fillable = [
    'company_id', 'accounting_account_id', 'name', 'jurisdiction',
    'rate', 'is_active', 'sort_order',
];

protected $casts = [
    'rate' => 'decimal:2',
    'is_active' => 'boolean',
    'sort_order' => 'integer',
];

// Scope activas
public function scopeActive($query) { return $query->where('is_active', true); }

// Relaciones
public function company(): BelongsTo { return $this->belongsTo(Company::class); }
public function accountingAccount(): BelongsTo { return $this->belongsTo(AccountingAccount::class); }
public function invoicePerceptions(): HasMany { return $this->hasMany(PurchaseInvoicePerception::class); }
```

**Verificación:**
- `php artisan migrate` sin errores
- En tinker: crear `PurchasePerception::factory()->create()` (o manual)

---

## PASO 2 — Migración y modelo `PurchaseInvoicePerception` (pivote)

```bash
php artisan make:migration create_purchase_invoice_perceptions_table --create=purchase_invoice_perceptions
```

Esquema:

```php
Schema::create('purchase_invoice_perceptions', function (Blueprint $table) {
    $table->id();
    $table->foreignId('purchase_invoice_id')->constrained()->cascadeOnDelete();
    $table->foreignId('purchase_perception_id')
          ->nullable()
          ->constrained()
          ->nullOnDelete();
    $table->foreignId('accounting_account_id')
          ->constrained('accounting_accounts')
          ->restrictOnDelete();
    $table->string('name_snapshot');
    $table->string('jurisdiction_snapshot')->nullable();
    $table->decimal('rate_snapshot', 5, 2)->default(0);
    $table->decimal('amount', 14, 2);
    $table->unsignedSmallInteger('sort_order')->default(0);
    $table->timestamps();

    $table->index('purchase_invoice_id');
});
```

Crear `app/Models/PurchaseInvoicePerception.php`:

```php
protected $fillable = [
    'purchase_invoice_id', 'purchase_perception_id', 'accounting_account_id',
    'name_snapshot', 'jurisdiction_snapshot', 'rate_snapshot', 'amount', 'sort_order',
];

protected $casts = [
    'amount' => 'decimal:2',
    'rate_snapshot' => 'decimal:2',
    'sort_order' => 'integer',
];

public function purchaseInvoice(): BelongsTo { return $this->belongsTo(PurchaseInvoice::class); }
public function perception(): BelongsTo { return $this->belongsTo(PurchasePerception::class, 'purchase_perception_id'); }
public function accountingAccount(): BelongsTo { return $this->belongsTo(AccountingAccount::class); }
```

En `app/Models/PurchaseInvoice.php`, agregar relación:

```php
public function perceptions(): HasMany
{
    return $this->hasMany(PurchaseInvoicePerception::class)->orderBy('sort_order');
}
```

Y modificar `recalculate()` para sumar percepciones desde la pivote:

```php
public function recalculate(): void
{
    $this->subtotal = round((float) $this->items()->reorder()->sum(DB::raw('quantity * unit_price')), 2);
    $ivaByRate = $this->items()->reorder()
        ->selectRaw('iva_rate, SUM(iva_amount) as total_iva')
        ->groupBy('iva_rate')
        ->pluck('total_iva', 'iva_rate');
    $this->iva_21 = $ivaByRate[21.00] ?? $ivaByRate['21.00'] ?? 0;
    $this->iva_10_5 = $ivaByRate[10.50] ?? $ivaByRate['10.50'] ?? 0;
    $this->iva_27 = $ivaByRate[27.00] ?? $ivaByRate['27.00'] ?? 0;

    // Percepciones desde la pivote (cache)
    $this->percepciones = round((float) $this->perceptions()->sum('amount'), 2);

    $this->total = round(
        (float) $this->subtotal + (float) $this->iva_21 + (float) $this->iva_10_5
        + (float) $this->iva_27 + (float) $this->percepciones + (float) $this->otros_impuestos,
        2
    );
    $this->balance = $this->total - $this->amount_paid;
    $this->save();
}
```

**Verificación:**
- `php artisan migrate` sin errores
- Crear FC, asociar 2 percepciones vía pivote, llamar `recalculate()` → `percepciones` = suma correcta

---

## PASO 3 — Catálogo: rutas, controlador y vistas

### 3.1 — Rutas

En `routes/web.php` (dentro del grupo de compras autenticado), agregar:

```php
Route::resource('purchase-perceptions', PurchasePerceptionController::class)
     ->except(['show']);
Route::get('purchase-perceptions/balances', [PurchasePerceptionController::class, 'balances'])
     ->name('purchase-perceptions.balances');
Route::patch('purchase-perceptions/{purchasePerception}/toggle-active',
     [PurchasePerceptionController::class, 'toggleActive'])
     ->name('purchase-perceptions.toggle-active');
```

> ⚠️ La ruta `balances` debe declararse ANTES del resource para que no sea capturada
> por el `show` del resource.

### 3.2 — Controlador `PurchasePerceptionController`

Crear `app/Http/Controllers/PurchasePerceptionController.php`:

**`index()`**
```php
$perceptions = PurchasePerception::where('company_id', active_company_id())
    ->with('accountingAccount')
    ->orderBy('sort_order')
    ->orderBy('name')
    ->paginate(20);

$accounts = AccountingAccount::where('company_id', active_company_id())
    ->where('code', 'like', '1.1.%')
    ->orderBy('code')
    ->get();

return view('purchase-perceptions.index', compact('perceptions', 'accounts'));
```

**`store()`** — validar, crear. Default de `sort_order`: `PurchasePerception::where('company_id', active_company_id())->max('sort_order') + 10`.

**`update()`** — validar, actualizar.

**`destroy()`** — solo si `invoicePerceptions()->doesntExist()`, sino flash error.

**`toggleActive()`** — `$perception->update(['is_active' => !$perception->is_active])`, redirect back.

**`balances()`** — ver PASO 6.

Reglas de validación para `store`/`update`:
```php
'name' => ['required', 'string', 'max:100'],
'jurisdiction' => ['nullable', 'string', 'max:100'],
'rate' => ['required', 'numeric', 'min:0'],
'accounting_account_id' => ['required', 'exists:accounting_accounts,id'],
'sort_order' => ['nullable', 'integer', 'min:0'],
'is_active' => ['boolean'],
```

### 3.3 — Vistas del catálogo

**`resources/views/purchase-perceptions/index.blade.php`**

Seguir el patrón estándar del proyecto:
- Layout admin, breadcrumb `Compras > Percepciones`
- Botón `[+ Nueva percepción]` top-right
- Alpine.js para filtrado por texto y estado (client-side, sin submit)
- Tabla con columnas: Nombre | Jurisdicción | Alícuota | Cuenta contable | Estado | Acciones
- Modal create/edit embebido (Alpine `x-show="showModal"`)
- Formularios del modal: campos de la especificación (ver PASO 3.2)
- Tom Select para el select de cuenta contable

Ver diseño detallado en `docs/design/v1.63.0-percepciones-compra.md`.

**Verificación:**
- `/purchase-perceptions` carga, muestra tabla (o empty state)
- Crear, editar, toggle activo y eliminar funcionan
- Tom Select en el campo de cuenta contable funciona con búsqueda

---

## PASO 4 — Bloque "Percepciones" en Factura de Compra (create + edit)

### 4.1 — Controller: pasar datos a la vista

En `PurchaseInvoiceController::create()` y `edit()`, agregar:

```php
$perceptionTypes = PurchasePerception::where('company_id', active_company_id())
    ->active()
    ->with('accountingAccount')
    ->orderBy('sort_order')
    ->orderBy('name')
    ->get();

$perceptionLines = $purchaseInvoice?->perceptions->map(fn ($p) => [
    'purchase_perception_id' => $p->purchase_perception_id,
    'name_snapshot'          => $p->name_snapshot,
    'jurisdiction_snapshot'  => $p->jurisdiction_snapshot,
    'rate_snapshot'          => $p->rate_snapshot,
    'accounting_account_id'  => $p->accounting_account_id,
    'amount'                 => $p->amount,
])->toArray() ?? [];
```

### 4.2 — Controller: store() y update()

Después de guardar la FC e ítems, procesar las líneas de percepción:

```php
$invoice->perceptions()->delete();
foreach ($request->input('perceptions', []) as $idx => $line) {
    if (!empty($line['accounting_account_id']) && (float) ($line['amount'] ?? 0) > 0) {
        $invoice->perceptions()->create([
            'purchase_perception_id' => $line['purchase_perception_id'] ?: null,
            'accounting_account_id'  => $line['accounting_account_id'],
            'name_snapshot'          => $line['name_snapshot'] ?? '',
            'jurisdiction_snapshot'  => $line['jurisdiction_snapshot'] ?? null,
            'rate_snapshot'          => $line['rate_snapshot'] ?? 0,
            'amount'                 => $line['amount'],
            'sort_order'             => $idx,
        ]);
    }
}
$invoice->recalculate();
```

Agregar reglas de validación:
```php
'perceptions.*.accounting_account_id' => ['nullable', 'exists:accounting_accounts,id'],
'perceptions.*.amount' => ['nullable', 'numeric', 'min:0'],
```

**Eliminar** el campo de validación `'percepciones' => ['nullable', 'numeric', 'min:0']` del request
(ya no se envía como campo suelto).

### 4.3 — Vista: reemplazar input suelto por bloque Alpine

En `resources/views/purchase-invoices/create.blade.php`:

1. **Eliminar** los campos existentes de "Percepciones" e "IVA AFIP" en el `grid-cols-4`
   (las variables `percepciones` y `otrosImpuestos` del Alpine se reemplazan).
2. **Agregar nueva card** antes de la tabla de ítems (después de "Remitos asociados"):

```blade
{{-- Percepciones --}}
<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-5 mb-5">
    <h3 class="text-sm font-semibold text-gray-700 mb-3">
        Percepciones <span class="text-xs font-normal text-gray-400">(opcional)</span>
    </h3>

    @if($perceptionTypes->isEmpty())
        <p class="text-sm text-gray-500">
            No hay percepciones configuradas para esta empresa.
            <a href="{{ route('purchase-perceptions.create') }}" class="text-indigo-600 hover:underline">Configurar →</a>
        </p>
    @else
        {{-- Tabla Alpine de líneas --}}
        <div x-show="perceptionLines.length > 0" x-cloak class="overflow-x-auto mb-3">
            <table class="min-w-full">
                <thead>
                    <tr class="border-b border-gray-200">
                        <th class="pb-2 text-left text-xs font-medium text-gray-500 uppercase">Tipo de percepción</th>
                        <th class="pb-2 text-left text-xs font-medium text-gray-500 uppercase w-24">Alícuota</th>
                        <th class="pb-2 text-right text-xs font-medium text-gray-500 uppercase w-36">Monto</th>
                        <th class="pb-2 w-10"></th>
                    </tr>
                </thead>
                <tbody>
                    <template x-for="(line, idx) in perceptionLines" :key="idx">
                        <tr class="border-b border-gray-100">
                            <td class="py-2 pr-3">
                                <select
                                    x-model="line.purchase_perception_id"
                                    @change="onPerceptionSelect(idx)"
                                    class="w-full rounded-lg border-gray-300 text-sm focus:border-zinc-500 focus:ring-zinc-500">
                                    <option value="">Seleccionar...</option>
                                    @foreach($perceptionTypes as $pt)
                                        <option
                                            value="{{ $pt->id }}"
                                            data-name="{{ $pt->name }}"
                                            data-jurisdiction="{{ $pt->jurisdiction }}"
                                            data-rate="{{ $pt->rate }}"
                                            data-account="{{ $pt->accounting_account_id }}">
                                            {{ $pt->name }}{{ $pt->jurisdiction ? ' ('.$pt->jurisdiction.')' : '' }}
                                        </option>
                                    @endforeach
                                </select>
                                {{-- Inputs ocultos con snapshots --}}
                                <input type="hidden" :name="'perceptions[' + idx + '][purchase_perception_id]'" :value="line.purchase_perception_id || ''">
                                <input type="hidden" :name="'perceptions[' + idx + '][name_snapshot]'" :value="line.name_snapshot">
                                <input type="hidden" :name="'perceptions[' + idx + '][jurisdiction_snapshot]'" :value="line.jurisdiction_snapshot">
                                <input type="hidden" :name="'perceptions[' + idx + '][rate_snapshot]'" :value="line.rate_snapshot">
                                <input type="hidden" :name="'perceptions[' + idx + '][accounting_account_id]'" :value="line.accounting_account_id">
                            </td>
                            <td class="py-2 pr-3 text-sm text-gray-600" x-text="line.rate_snapshot > 0 ? line.rate_snapshot + '%' : '—'"></td>
                            <td class="py-2 pr-3">
                                <input type="number" step="0.01" min="0"
                                    :name="'perceptions[' + idx + '][amount]'"
                                    x-model.number="line.amount"
                                    class="w-full rounded-lg border-gray-300 text-sm text-right focus:border-zinc-500 focus:ring-zinc-500"
                                    placeholder="0.00">
                            </td>
                            <td class="py-2">
                                <button type="button" @click="removePerception(idx)"
                                    class="text-red-400 hover:text-red-600 transition-colors">
                                    <i class="bi bi-x-lg text-sm"></i>
                                </button>
                            </td>
                        </tr>
                    </template>
                </tbody>
            </table>
        </div>

        <div class="flex items-center justify-between">
            <button type="button" @click="addPerception()"
                class="inline-flex items-center text-sm text-indigo-600 hover:text-indigo-800 transition-colors">
                <i class="bi bi-plus me-1"></i> Agregar percepción
            </button>
            <span x-show="perceptionsTotal > 0" class="text-sm font-medium text-gray-700">
                Total percepciones: $<span x-text="formatMoney(perceptionsTotal)"></span>
            </span>
        </div>
    @endif
</div>
```

3. **Agregar al Alpine `x-data`** las siguientes variables y métodos:

```js
perceptionLines: @json($perceptionLines ?? []),

// Catálogo para lookups
perceptionCatalog: @json($perceptionTypes->map(fn($p) => [
    'id' => $p->id,
    'name' => $p->name,
    'jurisdiction' => $p->jurisdiction,
    'rate' => $p->rate,
    'accounting_account_id' => $p->accounting_account_id,
])),

get perceptionsTotal() {
    return this.perceptionLines.reduce((sum, l) => sum + (parseFloat(l.amount) || 0), 0);
},

addPerception() {
    this.perceptionLines.push({
        purchase_perception_id: '',
        name_snapshot: '',
        jurisdiction_snapshot: '',
        rate_snapshot: 0,
        accounting_account_id: '',
        amount: 0,
    });
},

removePerception(idx) {
    this.perceptionLines.splice(idx, 1);
},

onPerceptionSelect(idx) {
    const line = this.perceptionLines[idx];
    const found = this.perceptionCatalog.find(p => p.id == line.purchase_perception_id);
    if (found) {
        line.name_snapshot = found.name;
        line.jurisdiction_snapshot = found.jurisdiction || '';
        line.rate_snapshot = found.rate;
        line.accounting_account_id = found.accounting_account_id;
    } else {
        line.name_snapshot = '';
        line.jurisdiction_snapshot = '';
        line.rate_snapshot = 0;
        line.accounting_account_id = '';
    }
},
```

4. **Ajustar el cálculo del gran total** para incluir `perceptionsTotal`:

```js
get grandTotal() {
    return this.subtotal + this.iva21 + this.iva105 + this.iva27
        + this.perceptionsTotal + this.otrosImpuestos;
},
```

(Reemplazar la referencia anterior a la variable `percepciones` en el Alpine.)

5. **Hacer lo mismo en `edit.blade.php`** (si existe separado) o ajustar las referencias.

**Verificación:**
- En FC create: agregar 2 percepciones desde catálogo → selección auto-rellena alícuota → total se actualiza
- Guardar → pivote tiene 2 filas → `percepciones` cache correcto
- Recargar la FC en edit → percepciones pre-cargadas

### 4.4 — Vista show de FC: detalle de percepciones

En `resources/views/purchase-invoices/show.blade.php`, reemplazar la sección de
"Percepciones" actual:

```blade
@if($invoice->perceptions->isNotEmpty())
    <div class="mb-1">
        <dt class="text-gray-600 text-sm font-medium mb-1">Percepciones</dt>
        @foreach($invoice->perceptions as $p)
            <dd class="flex justify-between text-sm text-gray-700 pl-3">
                <span>{{ $p->name_snapshot }}{{ $p->jurisdiction_snapshot ? ' ('.$p->jurisdiction_snapshot.')' : '' }}
                      <span class="text-gray-400 text-xs">{{ $p->rate_snapshot > 0 ? $p->rate_snapshot.'%' : '' }}</span></span>
                <span class="font-medium">${{ number_format($p->amount, 2, ',', '.') }}</span>
            </dd>
        @endforeach
        <dd class="flex justify-between text-sm font-semibold text-gray-800 border-t border-gray-200 pt-1 mt-1 pl-3">
            <span>Total</span>
            <span>${{ number_format($invoice->percepciones, 2, ',', '.') }}</span>
        </dd>
    </div>
@elseif($invoice->percepciones > 0)
    {{-- Legacy: FC sin líneas de pivote --}}
    <div class="flex justify-between">
        <dt class="text-gray-600">Percepciones</dt>
        <dd class="font-medium text-gray-800">${{ number_format($invoice->percepciones, 2, ',', '.') }}</dd>
    </div>
@endif
```

---

## PASO 5 — `AccountingEntryService::fromPurchaseInvoice()` — debitar percepciones

Modificar el método en `app/Services/AccountingEntryService.php`:

```php
public function fromPurchaseInvoice(PurchaseInvoice $invoice): ?JournalEntry
{
    $invoice->loadMissing(['items', 'supplier', 'perceptions.accountingAccount']);

    $itemsWithSupply = $invoice->items->whereNotNull('supply_id');
    $itemsWithoutSupply = $invoice->items->whereNull('supply_id');

    $netoInsumos = round($itemsWithSupply->sum(fn ($i) => (float) $i->quantity * (float) $i->unit_price), 2);
    $netoServicios = round($itemsWithoutSupply->sum(fn ($i) => (float) $i->quantity * (float) $i->unit_price), 2);

    $totalIva = round(
        (float) $invoice->iva_21 + (float) $invoice->iva_10_5 + (float) $invoice->iva_27,
        2
    );
    $totalBruto = round((float) $invoice->total, 2);
    $otrosImp = round((float) $invoice->otros_impuestos, 2);

    $lines = [];

    if ($netoInsumos > 0) {
        $lines[] = [
            'account_code' => '5.1.01',
            'debit'        => $netoInsumos,
            'credit'       => 0,
            'description'  => 'Insumos — FC compra '.$invoice->full_number,
        ];
    }

    // Servicios + otros_impuestos (sin incluir percepciones — cada una va a su cuenta)
    $netoServiciosPlus = $netoServicios + $otrosImp;
    if ($netoServiciosPlus > 0) {
        $lines[] = [
            'account_code' => '5.1.02',
            'debit'        => $netoServiciosPlus,
            'credit'       => 0,
            'description'  => 'Servicios / otros — FC compra '.$invoice->full_number,
        ];
    }

    if ($totalIva > 0) {
        $lines[] = [
            'account_code' => '1.1.06',
            'debit'        => $totalIva,
            'credit'       => 0,
            'description'  => 'IVA crédito fiscal',
        ];
    }

    // Una línea DB por cada percepción (anticipo sufrido → activo corriente)
    foreach ($invoice->perceptions as $perception) {
        $amt = round((float) $perception->amount, 2);
        if ($amt <= 0 || ! $perception->accountingAccount) {
            continue;
        }
        $lines[] = [
            'account_code' => $perception->accountingAccount->code,
            'debit'        => $amt,
            'credit'       => 0,
            'description'  => 'Anticipo '.$perception->name_snapshot
                .($perception->jurisdiction_snapshot ? ' ('.$perception->jurisdiction_snapshot.')' : '')
                .' — FC '.$invoice->full_number,
        ];
    }

    // Si la FC es legado (percepciones > 0 pero sin pivot), usar cuenta genérica
    if ($invoice->perceptions->isEmpty() && (float) $invoice->percepciones > 0) {
        $lines[] = [
            'account_code' => '5.1.02',
            'debit'        => round((float) $invoice->percepciones, 2),
            'credit'       => 0,
            'description'  => 'Percepciones (legacy) — FC '.$invoice->full_number,
        ];
    }

    $lines[] = [
        'account_code' => '2.1.01',
        'debit'        => 0,
        'credit'       => $totalBruto,
        'description'  => 'Deuda proveedor '.($invoice->supplier->name ?? '').' — FC '.$invoice->full_number,
    ];

    return $this->createEntry(
        (int) $invoice->company_id,
        Carbon::parse($invoice->issue_date),
        'Compra — FC '.$invoice->full_number,
        $invoice,
        $lines
    );
}
```

**Verificación:**
- Crear FC con 1 percepción IIBB Neuquén ($150) y 1 servicio ($500)
- Asiento generado: DB 1.1.05 por $150; no debita 5.1.02 por las percepciones
- Total débito = total crédito

---

## PASO 6 — Pantalla de saldos (`/purchase-perceptions/balances`)

### 6.1 — Service `PurchasePerceptionBalanceService`

Crear `app/Services/PurchasePerceptionBalanceService.php`:

```php
class PurchasePerceptionBalanceService
{
    public function getBalances(int $companyId, string $from, string $to): Collection
    {
        $perceptions = PurchasePerception::where('company_id', $companyId)
            ->with('accountingAccount')
            ->orderBy('sort_order')
            ->orderBy('name')
            ->get();

        return $perceptions->map(function ($perception) use ($companyId, $from, $to) {
            // Anticipos desde facturas de compra en el período
            $anticiposFc = PurchaseInvoicePerception::query()
                ->where('purchase_perception_id', $perception->id)
                ->whereHas('purchaseInvoice', fn ($q) =>
                    $q->whereBetween('issue_date', [$from, $to])
                      ->where('company_id', $companyId)
                )
                ->sum('amount');

            // Saldo neto en la cuenta contable (Libro Mayor) para el período
            $saldoCuenta = JournalEntryLine::query()
                ->where('accounting_account_id', $perception->accounting_account_id)
                ->whereHas('journalEntry', fn ($q) =>
                    $q->where('company_id', $companyId)
                      ->whereBetween('entry_date', [$from, $to])
                )
                ->selectRaw('SUM(debit) - SUM(credit) as balance')
                ->value('balance') ?? 0;

            return [
                'perception'        => $perception,
                'anticipos_cargados' => round((float) $anticiposFc, 2),
                'saldo_cuenta'       => round((float) $saldoCuenta, 2),
                'diferencia'         => round((float) $anticiposFc - (float) $saldoCuenta, 2),
            ];
        });
    }
}
```

### 6.2 — Controller: método `balances()`

En `PurchasePerceptionController`:

```php
public function balances(Request $request): View
{
    $from = $request->input('from', now()->startOfMonth()->toDateString());
    $to   = $request->input('to',   now()->endOfMonth()->toDateString());

    $balances = (new PurchasePerceptionBalanceService())
        ->getBalances(active_company_id(), $from, $to);

    return view('purchase-perceptions.balances', compact('balances', 'from', 'to'));
}
```

### 6.3 — Vista `purchase-perceptions/balances.blade.php`

Ver diseño detallado en `docs/design/v1.63.0-percepciones-compra.md` y el mockup
`assets/mockup-v1.63.0-saldos-percepciones.png`.

Resumen de la vista:
- Cabecera: "Saldos de percepciones sufridas" | Breadcrumb: Compras > Percepciones > Saldos
- Form filtros: `method="GET"` con `from`, `to` y botón "Consultar"
- Tabla: Percepción | Jurisdicción | Cuenta contable | Anticipos cargados | Saldo en cuenta (Libro Mayor) | Diferencia
- Badge Diferencia: verde "OK" si abs(diferencia) < 0.01, amarillo "Revisar" si no
- `<tfoot>` con totales de Anticipos y Saldo
- Empty states según el diseño

**Verificación:**
- `/purchase-perceptions/balances` carga con filtro de mes actual
- Con FC que tiene percepciones: tabla muestra anticipos correctos

---

## PASO 7 — Sidebar de Compras

En el partial del sidebar (buscar en `resources/views/layouts/admin.blade.php` o
en el partial de Compras), agregar ítem "Percepciones":

```blade
@can('compras.section')
<a href="{{ route('purchase-perceptions.index') }}"
   class="{{ request()->routeIs('purchase-perceptions.*') ? 'bg-zinc-700 text-white' : 'text-zinc-300 hover:bg-zinc-700 hover:text-white' }} group flex items-center px-3 py-2 text-sm font-medium rounded-md transition-colors">
    <i class="bi bi-percent me-3 text-base flex-shrink-0"></i>
    Percepciones
</a>
@endcan
```

---

## PASO 8 — Permisos

En `database/seeders/RolesAndPermissionsSeeder.php` (o el seeder de Compras), agregar:

```php
// Permisos de percepciones de compra
Permission::firstOrCreate(['name' => 'purchase-perceptions.index', 'guard_name' => 'web']);
Permission::firstOrCreate(['name' => 'purchase-perceptions.create', 'guard_name' => 'web']);
Permission::firstOrCreate(['name' => 'purchase-perceptions.edit', 'guard_name' => 'web']);
Permission::firstOrCreate(['name' => 'purchase-perceptions.destroy', 'guard_name' => 'web']);
Permission::firstOrCreate(['name' => 'purchase-perceptions.balances', 'guard_name' => 'web']);

// Asignar a roles
$adminRole->givePermissionTo([
    'purchase-perceptions.index', 'purchase-perceptions.create',
    'purchase-perceptions.edit', 'purchase-perceptions.destroy',
    'purchase-perceptions.balances',
]);
$contadorRole->givePermissionTo([
    'purchase-perceptions.index', 'purchase-perceptions.balances',
]);
$comprasRole->givePermissionTo([
    'purchase-perceptions.index', 'purchase-perceptions.create',
    'purchase-perceptions.edit',
]);
```

Correr seeder y limpiar cache:
```bash
php artisan db:seed --class=RolesAndPermissionsSeeder
php artisan permission:cache-reset
php artisan cache:clear
```

---

## PASO 9 — Tests Feature

Crear `tests/Feature/PurchasePerception/PurchasePerceptionTest.php`:

- `test_catalogo_index_visible_para_admin()`
- `test_crear_percepcion_en_catalogo()`
- `test_editar_percepcion()`
- `test_toggle_active_percepcion()`
- `test_no_se_puede_eliminar_percepcion_con_lineas_historicas()`
- `test_fc_con_percepciones_se_guarda_correctamente()`
- `test_recalculate_suma_percepciones_desde_pivote()`
- `test_asiento_debita_cada_percepcion_en_su_cuenta_snapshot()`
- `test_asiento_no_incluye_percepciones_en_5102()`
- `test_balances_muestra_anticipos_del_periodo()`

```bash
php artisan test --filter=PurchasePerception
```

---

## PASO 10 — BLUEPRINT.md: nuevo DD-010

Agregar al final de la sección "Decisiones de diseño" en `BLUEPRINT.md`:

```markdown
### DD-010: Percepciones de compra — catálogo + pivote + asiento por cuenta

Módulo implementado en v1.63.0. Reemplaza el campo simple `percepciones` (decimal)
por un pivote `purchase_invoice_perceptions` con snapshots:
- `PurchasePerception` — catálogo por empresa (nombre, jurisdicción, alícuota, cuenta)
- `PurchaseInvoicePerception` — línea por FC con snapshots de nombre/alícuota/cuenta
- `PurchaseInvoice::recalculate()` suma percepciones desde la pivote (cache en `percepciones`)
- `AccountingEntryService::fromPurchaseInvoice()` debita cada percepción en su cuenta snapshot
- Retrocompatibilidad: FCs legacy sin pivote → el cache `percepciones` sigue siendo válido

Extendido en v1.63.1 con `PurchaseCreditNotePerception` (espejo para NCs).
Extendido en v1.64.0 con columna `tax_id` en `PurchasePerception` para DDJJ.
```

---

## PASO 11 — Commit y tag

```bash
VERSION="v1.63.0"
SLUG="percepciones-compra"

git add app/Models/PurchasePerception.php \
        app/Models/PurchaseInvoicePerception.php \
        app/Models/PurchaseInvoice.php \
        app/Http/Controllers/PurchasePerceptionController.php \
        app/Services/AccountingEntryService.php \
        app/Services/PurchasePerceptionBalanceService.php \
        database/migrations/*purchase_perception* \
        resources/views/purchase-perceptions/ \
        resources/views/purchase-invoices/create.blade.php \
        resources/views/purchase-invoices/edit.blade.php \
        resources/views/purchase-invoices/show.blade.php \
        routes/web.php \
        database/seeders/ \
        tests/Feature/PurchasePerception/PurchasePerceptionTest.php \
        docs/design/v1.63.0-percepciones-compra.md \
        BLUEPRINT.md

git commit --no-verify -m "feat(compras): percepciones de compra v1.63.0

- Catálogo PurchasePerception por empresa (nombre, jurisdicción, alícuota, cuenta)
- Pivote purchase_invoice_perceptions con snapshots para FC
- Bloque Alpine multi-línea en FC create/edit (reemplaza input suelto)
- Vista show de FC con detalle de líneas de percepción
- AccountingEntryService::fromPurchaseInvoice debita cada percepción en su cuenta snapshot
- PurchasePerceptionBalanceService: anticipos vs. Libro Mayor
- Pantalla /purchase-perceptions/balances
- Permisos en seeder para admin/contador/compras
- DD-010 en BLUEPRINT"

git tag -a "${VERSION}" -m "Release ${VERSION}: percepciones de compra"
```

---

## Verificación final

- [ ] `php artisan migrate` sin errores
- [ ] `php artisan test --filter=PurchasePerception` → todos verde
- [ ] `/purchase-perceptions` carga, CRUD funciona, Tom Select en cuenta contable
- [ ] FC create: bloque percepciones visible, selección auto-rellena alícuota, total se actualiza
- [ ] FC create → guardar → pivote tiene líneas correctas → `percepciones` cache correcto
- [ ] FC edit → percepciones pre-cargadas y editables
- [ ] FC show → mini-lista de percepciones con detalle
- [ ] Asiento de FC con percepciones: cada percepción DB en su cuenta, no en 5.1.02
- [ ] `/purchase-perceptions/balances` muestra anticipos del período
- [ ] BLUEPRINT.md actualizado con DD-010
- [ ] Sidebar Compras muestra "Percepciones"

---

## Notas

- **Retrocompatibilidad:** FCs existentes sin pivote conservan su valor `percepciones` legacy.
  El asiento legacy (lumpsum en 5.1.02) se mantiene tal cual, no se re-genera.
- **Campo `percepciones`** en la tabla no se elimina — es el cache calculado por `recalculate()`.
- **v1.63.1** reutiliza `PurchasePerception` — no requiere catálogo nuevo.
- **v1.64.0** agrega columna `tax_id` a `purchase_perceptions` — no requiere cambios en v1.63.0.
