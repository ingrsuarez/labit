# v1.45.0 — Eliminar cliente: solo bloquear por protocolos o facturación

**DEPENDENCIAS:** ninguna (v1.44.0 en develop)

## Objetivo

Ajustar `CustomerController::destroy` para que la eliminación esté permitida **solo cuando el cliente no tenga**:

1. **Protocolos:** muestras (aguas/alimentos) y/o protocolos veterinarios (`VetAdmission`).
2. **Facturación:** facturas de venta, recibos de cobro y/o notas de crédito vinculadas al cliente.

Mensajes de error agrupados en esos dos conceptos (español claro). Actualizar texto de ayuda en `customer/edit`.

## PASO 1 (prompt) — Implementación

- Refactorizar comprobaciones en `destroy`; mantener integridad con FK (`samples` sigue en restrict).
- Tests Feature que cubran: borrado OK sin relaciones; bloqueo con protocolo vet; bloqueo con muestra; bloqueo con factura.

## PASO 2 — Verificación

- `vendor/bin/pint --test`
- `php artisan test`

## PASO 3 — Documentación

- Entrada en `CHANGELOG.md` para v1.45.0
- `ROADMAP.md` y `STATUS.md` si aplica

## Tag y merge

- Tag `v1.45.0` tras merge a develop según AGENTE_WORKFLOW.md
