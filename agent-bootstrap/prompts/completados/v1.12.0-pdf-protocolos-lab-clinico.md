# v1.12.0 — PDF de protocolos de laboratorio clínico + envío por email

**VERSIÓN:** v1.12.0  
**SLUG:** pdf-protocolos-lab-clinico  
**DEPENDENCIAS:** v1.11.1 (config de correos, para el envío por email)  
**ESTIMACIÓN:** 3-4h  
**PRIORIDAD:** alta

---

## Descripción

Crear la generación de informes PDF para protocolos de laboratorio clínico (admisiones)
siguiendo el mismo formato tabular del PDF de muestras. Incluye descarga, vista en
navegador y envío por email con PDF adjunto.

**Título del PDF:** "Laboratorio de Análisis Clínicos" (NO "Laboratorio de Aguas y Alimentos").

---

## PASO 0 — Contexto previo a leer

Leer sin ejecutar nada:

- `resources/views/sample/pdf-mpdf.blade.php` — **template de referencia completo** (634 líneas).
  Este es el diseño a replicar adaptando los datos de admisión
- `app/Http/Controllers/SampleController.php` — métodos `downloadPdf()` y `viewPdf()`
  (líneas 695-762) y `generatePdfFilename()` como referencia
- `app/Http/Controllers/LabAdmissionController.php` — método `show()` (líneas 287-304).
  Actualmente NO tiene métodos de PDF ni email
- `app/Models/Admission.php` — modelo, relaciones, campos
- `app/Models/AdmissionTest.php` — modelo con `is_validated`, `validated_by`, `validated_at`,
  `result`, `unit`, `reference_value`
- `app/Mail/SampleResultMail.php` — Mailable de referencia con PDF adjunto (v1.9.0)
- `resources/views/lab/admissions/show.blade.php` — vista del protocolo, para agregar botones
- `resources/views/lab/admissions/index.blade.php` — listado, para agregar enlace PDF por fila
- `routes/web.php` — rutas de `lab/admissions` (líneas 138-161)
- `app/Models/LabSetting.php` — si existe, para obtener config de email de resultados

### Diferencias clave con el PDF de muestras

| Aspecto | Muestras (Sample) | Lab Clínico (Admission) |
|---|---|---|
| Modelo principal | `Sample` | `Admission` |
| Determinaciones | `SampleDetermination` | `AdmissionTest` |
| Validación | `Sample.validation_status` | `AdmissionTest.is_validated` (por práctica) |
| Validador | `Sample.validated_by` → User | `AdmissionTest.validated_by` → User |
| Resultado | `SampleDetermination.result` | `AdmissionTest.result` |
| Unidad | `SampleDetermination` vía `test.unit` | `AdmissionTest.unit` |
| Referencia | `SampleDetermination` vía ref category | `AdmissionTest.reference_value` |
| Paciente | `Sample.customer` (Customer) | `Admission.patient` (Patient) |
| Obra social | No aplica | `Admission.insuranceRelation` |
| Título lab | "Laboratorio de Aguas y Alimentos" | **"Laboratorio de Análisis Clínicos"** |

---

## PASO 1 — Rutas

En `routes/web.php`, dentro del grupo `lab/admissions`:

```php
Route::get('lab/admissions/{admission}/pdf/download', [LabAdmissionController::class, 'downloadPdf'])
    ->name('lab.admissions.pdf.download');
Route::get('lab/admissions/{admission}/pdf/view', [LabAdmissionController::class, 'viewPdf'])
    ->name('lab.admissions.pdf.view');
Route::post('lab/admissions/{admission}/send-email', [LabAdmissionController::class, 'sendEmail'])
    ->name('lab.admissions.sendEmail');
```

---

## PASO 2 — Template PDF

Crear `resources/views/lab/admissions/pdf-mpdf.blade.php`.

### Estructura (replicar `sample/pdf-mpdf.blade.php` con estas adaptaciones):

**Header (`<htmlpageheader>`):**
- Logo IPAC a la izquierda
- Título: **"LABORATORIO DE ANÁLISIS CLÍNICOS"** (en vez de "Aguas y Alimentos")
- Subtítulo: "Informe de Resultados"

**Barra de información del paciente:**
- Paciente: `$admission->patient->name` + `$admission->patient->last_name`
- DNI: `$admission->patient->dni`
- Obra Social: `$admission->insuranceRelation->name`
- Nro. Afiliado: `$admission->affiliate_number`
- Médico Solicitante: `$admission->requesting_doctor`
- Fecha: `$admission->date`
- Protocolo: `$admission->protocol_number`

**Tabla de determinaciones (solo las validadas):**
Filtrar `$admission->admissionTests` donde `is_validated === true`.

Organizar por categorías/padres igual que en muestras:
- Padres (tests con hijos): fila bold con nombre del padre
  - Método del padre (si tiene): fila italic debajo
  - Hijos: filas indentadas con Análisis | Resultado | Unidad | Referencia
- Tests standalone (sin padre): fila normal con las 4 columnas

Columnas de la tabla:
| Análisis | Resultado | Unidad | Valores de Referencia |

Para cada `AdmissionTest`:
- Análisis: `$admissionTest->test->name`
- Resultado: `$admissionTest->result`
- Unidad: `$admissionTest->unit` (o `$admissionTest->test->unit`)
- Referencia: `$admissionTest->reference_value`

**Observaciones:** `$admission->observations` (si tiene)

**Sección de validación (pie):**
- "RESULTADO VALIDADO POR SISTEMA DIGITAL"
- Datos de contacto IPAC
- Firma del validador (imagen si existe) + nombre
- Para determinar el validador: buscar el User que validó la mayoría de las prácticas,
  o el último que validó. Ejemplo:
  ```php
  $validatorId = $admission->admissionTests
      ->where('is_validated', true)
      ->pluck('validated_by')
      ->countBy()
      ->sortDesc()
      ->keys()
      ->first();
  $validator = $validatorId ? User::find($validatorId) : null;
  ```

**Footer (`<htmlpagefooter>`):**
- Protocolo: `$admission->protocol_number`
- Página X de Y
- "Generado: fecha/hora"

### Estilos CSS

Copiar los estilos de `sample/pdf-mpdf.blade.php` sin cambios (mismas clases:
`.det-table`, `.det-section-header`, `.det-parent`, `.det-method`, `.det-child`,
`.det-standalone`, `.det-result`, `.det-unit`, `.det-ref`, `.validation-section`, etc.)

---

## PASO 3 — Métodos en LabAdmissionController

### 3.1 — downloadPdf()

```php
public function downloadPdf(Admission $admission)
{
    $this->authorize('lab-admissions.show');

    $validatedCount = $admission->admissionTests()->where('is_validated', true)->count();
    if ($validatedCount === 0) {
        return back()->with('error', 'Debe validar al menos una determinación para descargar el informe.');
    }

    $admission->load([
        'patient',
        'insuranceRelation',
        'admissionTests' => fn($q) => $q->where('is_validated', true),
        'admissionTests.test.parentTests',
        'admissionTests.test.childTests',
        'admissionTests.test.referenceValues.category',
        'creator',
    ]);

    // Determinar el validador principal
    $validatorId = $admission->admissionTests
        ->pluck('validated_by')
        ->countBy()->sortDesc()->keys()->first();
    $validator = $validatorId ? \App\Models\User::find($validatorId) : null;

    $pdf = PDF::loadView('lab.admissions.pdf-mpdf', compact('admission', 'validator'), [], [
        'margin_top' => 35,
        'margin_bottom' => 20,
        'margin_left' => 15,
        'margin_right' => 15,
    ]);

    return $pdf->download($this->generatePdfFilename($admission));
}
```

### 3.2 — viewPdf()

Igual a downloadPdf pero con `$pdf->stream(...)`.

### 3.3 — generatePdfFilename()

```php
private function generatePdfFilename(Admission $admission): string
{
    $parts = [
        'LabClinico',
        $admission->patient?->name ?? 'SinPaciente',
        $admission->date ? $admission->date->format('Y-m-d') : now()->format('Y-m-d'),
    ];

    $sanitized = collect($parts)->map(function ($part) {
        $clean = iconv('UTF-8', 'ASCII//TRANSLIT//IGNORE', $part);
        $clean = preg_replace('/[^A-Za-z0-9_-]/', '_', $clean);
        $clean = preg_replace('/_+/', '_', $clean);
        return trim($clean, '_');
    })->implode('-');

    return $sanitized . '.' . $admission->protocol_number . '.pdf';
}
```

### 3.4 — sendEmail()

```php
public function sendEmail(Request $request, Admission $admission)
{
    $this->authorize('lab-admissions.show');

    $validatedCount = $admission->admissionTests()->where('is_validated', true)->count();
    if ($validatedCount === 0) {
        return back()->with('error', 'Debe validar al menos una determinación para enviar el informe.');
    }

    $validated = $request->validate([
        'email' => 'required|email',
        'message' => 'nullable|string',
    ]);

    $fromEmail = LabSetting::get('results_email', config('mail.from.address'));
    $fromName = LabSetting::get('results_from_name', config('mail.from.name'));

    Mail::mailer('smtp')
        ->to($validated['email'])
        ->send(
            (new AdmissionResultMail($admission, $validated['message'] ?? null))
                ->from($fromEmail, $fromName)
        );

    return back()->with('success', 'Informe enviado correctamente a ' . $validated['email']);
}
```

---

## PASO 4 — Mailable AdmissionResultMail

Crear `app/Mail/AdmissionResultMail.php`, siguiendo el patrón de `SampleResultMail`:

- Constructor: recibe `Admission $admission` y `?string $customMessage`
- Envelope: asunto desde `LabSetting::get('results_default_subject')` con `{protocol}` reemplazado
- Content: vista `emails.admission-result`
- Attachments: PDF generado con mPDF, nombre descriptivo

Crear `resources/views/emails/admission-result.blade.php`:
- Mismo formato que `emails/sample-result.blade.php`
- Texto: "Adjunto encontrará el informe de resultados del protocolo {protocol_number}"
- Firma HTML desde LabSetting

---

## PASO 5 — Botones en las vistas

### 5.1 — Vista show (`lab/admissions/show.blade.php`)

Agregar botones de PDF y email en el header del protocolo, visibles solo si hay
al menos 1 determinación validada:

```html
@php
    $validatedCount = $admission->admissionTests->where('is_validated', true)->count();
@endphp

@if($validatedCount > 0)
    <a href="{{ route('lab.admissions.pdf.view', $admission) }}" target="_blank"
       class="inline-flex items-center px-3 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 text-sm">
        <svg class="w-4 h-4 mr-1" ...><!-- ícono impresora --></svg>
        Ver PDF
    </a>
    <a href="{{ route('lab.admissions.pdf.download', $admission) }}"
       class="inline-flex items-center px-3 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 text-sm">
        <svg class="w-4 h-4 mr-1" ...><!-- ícono descarga --></svg>
        Descargar PDF
    </a>
    <!-- Botón email con modal o form inline -->
@endif
```

### 5.2 — Vista index (`lab/admissions/index.blade.php`)

En la columna "Acciones", agregar enlace PDF junto a "Ver":

```html
@php
    $valCount = $admission->admissionTests->where('is_validated', true)->count();
@endphp
<a href="{{ route('lab.admissions.show', $admission) }}" class="text-teal-600 hover:text-teal-900">Ver</a>
@if($valCount > 0)
    <a href="{{ route('lab.admissions.pdf.view', $admission) }}" target="_blank"
       class="text-green-600 hover:text-green-900" title="Ver PDF ({{ $valCount }} validadas)">
        PDF
    </a>
@endif
```

**IMPORTANTE:** Verificar que el controller `index()` carga `admissionTests` con
eager loading para evitar N+1 queries:
```php
$admissions = Admission::with(['patient', 'insuranceRelation', 'admissionTests'])
    ->orderBy(...)
```

---

## PASO 6 — Verificación funcional

### Test 1: Descargar PDF
1. Ir a un protocolo con determinaciones validadas
2. Click "Descargar PDF"
3. Se descarga con nombre `LabClinico-Silvina-2026-03-23.2026-000003.pdf`
4. El PDF muestra:
   - Header: "LABORATORIO DE ANÁLISIS CLÍNICOS"
   - Datos del paciente, obra social, médico
   - Tabla con determinaciones validadas (padres/hijos/standalone)
   - Resultado, unidad, referencia en cada fila
   - Firma del validador
   - Footer con protocolo y página

### Test 2: Ver PDF en navegador
1. Click "Ver PDF" → se abre en nueva pestaña
2. El contenido es idéntico al descargado

### Test 3: PDF sin determinaciones validadas
1. Ir a un protocolo sin validaciones
2. Los botones PDF NO deben aparecer
3. Si se accede por URL directa → error con mensaje

### Test 4: Enviar por email
1. Click "Enviar por Email" en la vista show
2. Ingresar email → envía con PDF adjunto
3. Verificar remitente, asunto y firma desde LabSetting

### Test 5: PDF en listado index
1. En el listado de admisiones, los protocolos con validaciones muestran enlace "PDF"
2. Los que no tienen validaciones solo muestran "Ver"

### Test 6: Formato de tabla
1. Protocolo con padres e hijos → padres bold, hijos indentados
2. Protocolo con standalone → filas normales
3. Método del test → aparece en italic debajo del padre

### Test 7: Firma del validador
1. Validador con firma subida → aparece imagen + nombre
2. Validador sin firma → solo nombre

---

## PASO N — Commit y tag

```bash
VERSION="v1.12.0"
SLUG="pdf-protocolos-lab-clinico"

git add app/Http/Controllers/LabAdmissionController.php \
        app/Mail/AdmissionResultMail.php \
        resources/views/lab/admissions/pdf-mpdf.blade.php \
        resources/views/lab/admissions/show.blade.php \
        resources/views/lab/admissions/index.blade.php \
        resources/views/emails/admission-result.blade.php \
        routes/web.php

git commit -m "feat(lab-clinico): PDF de protocolos + envío por email

- Template PDF mPDF con titulo Laboratorio de Análisis Clínicos
- Formato tabular: categorías, padres/hijos, resultado/unidad/ref
- Firma del validador con imagen si existe
- Descarga y vista en navegador
- Envío por email con PDF adjunto usando LabSetting
- Nombre descriptivo: LabClinico-Paciente-Fecha.Protocolo.pdf
- Botones PDF en show y index"

git tag -a "${VERSION}" -m "Release v1.12.0: PDF de protocolos de laboratorio clínico"
```

---

## Instrucciones de producción

```bash
php artisan view:clear
```

No requiere migraciones. Requiere que v1.11.1 (config correos) esté completada
para el envío por email (sin ella, el PDF funciona pero el email usa fallback de .env).

---

## Verificación final

- [ ] PDF se genera correctamente con mPDF
- [ ] Header dice "Laboratorio de Análisis Clínicos"
- [ ] Datos del paciente, obra social, médico en la barra de info
- [ ] Solo se muestran determinaciones validadas
- [ ] Padres bold, hijos indentados, métodos en italic
- [ ] Columnas: Análisis | Resultado | Unidad | Valores de Referencia
- [ ] Firma del validador (imagen + nombre)
- [ ] Footer con protocolo y paginación
- [ ] Nombre de archivo descriptivo
- [ ] Botón PDF visible solo con validaciones
- [ ] Botón PDF en listado index
- [ ] Envío por email funciona con PDF adjunto
- [ ] Sin validaciones: botones ocultos, URL directa da error
- [ ] No hay N+1 queries en el index
