# v1.25.0 — PDF de resultados veterinarios + envío por email

**VERSIÓN:** v1.25.0  
**SLUG:** pdf-email-veterinario  
**DEPENDENCIAS:** v1.24.0  
**ESTIMACIÓN:** 2h  
**PRIORIDAD:** alta

---

## Descripción

Genera el PDF de resultados para protocolos veterinarios y permite enviarlo por email al dueño
del animal, al veterinario derivante, o a la veterinaria. Sigue el patrón existente del PDF
clínico (`lab/admissions/pdf-mpdf.blade.php`) adaptado al contexto veterinario.

El PDF incluye: datos de la veterinaria, datos del animal (nombre, especie, raza, edad),
datos del dueño, veterinario derivante, tabla de determinaciones con resultados y valores
de referencia por especie, firma del validador.

---

## PASO 0 — Contexto previo a leer

> ⚠️ Si venís del AGENTE_WORKFLOW.md, saltear este paso — ya lo hiciste.

Leer sin ejecutar nada:
- `app/Http/Controllers/LabAdmissionController.php` — métodos `downloadPdf`, `viewPdf`, `sendEmail`, `generatePdfFilename` (patrón a seguir)
- `resources/views/lab/admissions/pdf-mpdf.blade.php` — template del PDF clínico (patrón visual)
- `app/Mail/AdmissionResultMail.php` — Mailable del clínico (patrón para email)
- `app/Models/LabSetting.php` — configuración de correo del laboratorio
- `app/Models/VetAdmission.php` — modelo del protocolo veterinario (v1.24.0)
- `app/Models/VetAdmissionTest.php` — modelo de determinaciones del protocolo vet (v1.24.0)
- `resources/views/vet/admissions/show.blade.php` — vista show (agregar botones de PDF y email)

---

## PASO 1 — Template PDF: `resources/views/vet/admissions/pdf-mpdf.blade.php`

Crear basándose en el template del clínico (`lab/admissions/pdf-mpdf.blade.php`) con estas adaptaciones:

### 1.1 — Header del PDF

En vez de datos del paciente, mostrar:

```html
<div class="sample-info-bar">
    <div class="sample-info-row">
        <strong>Protocolo:</strong> {{ $vetAdmission->protocol_number }}
        &nbsp;&nbsp;&nbsp;
        <strong>Fecha:</strong> {{ $vetAdmission->date->format('d/m/Y') }}
    </div>
    <div class="sample-info-row">
        <strong>Animal:</strong> {{ $vetAdmission->animal_name }}
        &nbsp;&nbsp;&nbsp;
        <strong>Especie:</strong> {{ $vetAdmission->species->name }}
        @if($vetAdmission->breed)
            &nbsp;&nbsp;&nbsp;
            <strong>Raza:</strong> {{ $vetAdmission->breed }}
        @endif
        @if($vetAdmission->age)
            &nbsp;&nbsp;&nbsp;
            <strong>Edad:</strong> {{ $vetAdmission->age }}
        @endif
    </div>
    <div class="sample-info-row">
        <strong>Propietario:</strong> {{ $vetAdmission->owner_name }}
        @if($vetAdmission->owner_phone)
            &nbsp;&nbsp;&nbsp;
            <strong>Tel:</strong> {{ $vetAdmission->owner_phone }}
        @endif
    </div>
    <div class="sample-info-row">
        <strong>Veterinaria:</strong> {{ $vetAdmission->customer->name }}
        @if($vetAdmission->veterinarian)
            &nbsp;&nbsp;&nbsp;
            <strong>Derivante:</strong> {{ $vetAdmission->veterinarian->name }}
            @if($vetAdmission->veterinarian->matricula)
                (MP {{ $vetAdmission->veterinarian->matricula }})
            @endif
        @endif
    </div>
</div>
```

### 1.2 — Tabla de Resultados

Misma estructura tabular que el clínico:
- Columnas: Análisis | Resultado | Unidad | Valor de Referencia
- Tests padre en bold con fondo gris
- Tests hijos indentados (sub-padres con doble indentación si aplica)
- Solo mostrar determinaciones validadas (`is_validated = true`)
- El valor de referencia ya viene congelado en `reference_value` del `VetAdmissionTest`

### 1.3 — Pie de página

- Firma del validador (imagen si tiene firma cargada, igual que clínico)
- Nombre y matrícula del validador
- Texto: "Los resultados corresponden a la muestra recibida"
- Fecha de validación

### 1.4 — Estilos

Reutilizar los mismos estilos del PDF clínico. Usar colores amber/warm si se quiere
distinguir visualmente del clínico, o mantener el mismo estilo corporativo.

**Verificación:** Generar un PDF de prueba y verificar que se ve correctamente.

---

## PASO 2 — Métodos en VetAdmissionController

Agregar los siguientes métodos (seguir el patrón exacto de `LabAdmissionController`):

### 2.1 — `downloadPdf(VetAdmission $vetAdmission)`

```php
public function downloadPdf(VetAdmission $vetAdmission)
{
    $validatedCount = $vetAdmission->vetTests()->where('is_validated', true)->count();
    if ($validatedCount === 0) {
        return back()->with('error', 'Debe validar al menos una determinación para descargar el informe.');
    }

    $vetAdmission->load([
        'customer', 'veterinarian', 'species',
        'vetTests.test.parentTests', 'vetTests.test.childTests',
    ]);

    $validatorId = $vetAdmission->vetTests
        ->where('is_validated', true)
        ->pluck('validated_by')
        ->countBy()->sortDesc()->keys()->first();
    $validator = $validatorId ? \App\Models\User::find($validatorId) : null;

    $pdf = PDF::loadView('vet.admissions.pdf-mpdf', compact('vetAdmission', 'validator'), [], [
        'margin_top' => 35,
        'margin_bottom' => 20,
        'margin_left' => 15,
        'margin_right' => 15,
    ]);

    return $pdf->download($this->generatePdfFilename($vetAdmission));
}
```

### 2.2 — `viewPdf(VetAdmission $vetAdmission)`

Igual que `downloadPdf` pero usa `$pdf->stream(...)` en vez de `$pdf->download(...)`.

### 2.3 — `sendEmail(Request $request, VetAdmission $vetAdmission)`

```php
public function sendEmail(Request $request, VetAdmission $vetAdmission)
{
    $validatedCount = $vetAdmission->vetTests()->where('is_validated', true)->count();
    if ($validatedCount === 0) {
        return back()->with('error', 'Debe validar al menos una determinación para enviar el informe.');
    }

    $validated = $request->validate([
        'email' => 'required|email',
        'message' => 'nullable|string',
    ]);

    $fromEmail = LabSetting::get('results_email', config('mail.from.address'));
    $fromName = LabSetting::get('results_from_name', config('mail.from.name'));

    Mail::mailer('smtp')
        ->to($validated['email'])
        ->send(
            (new VetAdmissionResultMail($vetAdmission, $validated['message'] ?? null))
                ->from($fromEmail, $fromName)
        );

    return back()->with('success', 'Informe enviado correctamente a ' . $validated['email']);
}
```

### 2.4 — `generatePdfFilename(VetAdmission $vetAdmission)`

```php
private function generatePdfFilename(VetAdmission $vetAdmission): string
{
    $parts = [
        'LabVeterinario',
        $vetAdmission->animal_name,
        $vetAdmission->species->name ?? 'SinEspecie',
        $vetAdmission->date ? $vetAdmission->date->format('Y-m-d') : now()->format('Y-m-d'),
    ];

    $sanitized = collect($parts)->map(function ($part) {
        $clean = iconv('UTF-8', 'ASCII//TRANSLIT//IGNORE', $part);
        $clean = preg_replace('/[^A-Za-z0-9_-]/', '_', $clean);
        $clean = preg_replace('/_+/', '_', $clean);
        return trim($clean, '_');
    })->implode('-');

    return $sanitized . '.' . $vetAdmission->protocol_number . '.pdf';
}
```

**Verificación:** Los 3 métodos funcionan: descargar, visualizar y enviar por email.

---

## PASO 3 — Mailable: `VetAdmissionResultMail`

Crear `app/Mail/VetAdmissionResultMail.php` siguiendo el patrón de `AdmissionResultMail`:

```bash
php artisan make:mail VetAdmissionResultMail
```

El Mailable debe:
1. Recibir `VetAdmission $vetAdmission` y `?string $customMessage`
2. Generar el PDF en memoria y adjuntarlo
3. Usar la vista de email (puede ser la misma genérica o una nueva `emails.vet-admission-result`)
4. Asunto: `"Resultados Lab Veterinario - {animal_name} ({species}) - Protocolo {protocol_number}"`
5. Configurar `from` con LabSetting (correo configurado del laboratorio)

**Verificación:** Enviar un email de prueba y verificar que llega con el PDF adjunto.

---

## PASO 4 — Vista de email (opcional, puede reutilizar)

Si `AdmissionResultMail` usa una vista genérica que sirve, reutilizarla. Si no, crear
`resources/views/emails/vet-admission-result.blade.php` con:

- Saludo con nombre del dueño del animal
- Texto: "Adjuntamos los resultados del análisis de {animal_name} ({especie})"
- Protocolo número
- Mensaje personalizado (si se envió)
- Firma del laboratorio (desde LabSetting `results_signature`)

**Verificación:** El email se renderiza correctamente con el contenido esperado.

---

## PASO 5 — Rutas para PDF y email

Agregar dentro del grupo `vet` en `routes/web.php`:

```php
Route::get('admissions/{vetAdmission}/pdf', [VetAdmissionController::class, 'downloadPdf'])->name('vet.admissions.downloadPdf');
Route::get('admissions/{vetAdmission}/pdf/view', [VetAdmissionController::class, 'viewPdf'])->name('vet.admissions.viewPdf');
Route::post('admissions/{vetAdmission}/send-email', [VetAdmissionController::class, 'sendEmail'])->name('vet.admissions.sendEmail');
```

**Verificación:** `php artisan route:list --path=vet` muestra las rutas de PDF y email.

---

## PASO 6 — Botones en vista show

En `resources/views/vet/admissions/show.blade.php`, agregar botones de acción:

### 6.1 — Botones de PDF (solo si hay al menos una determinación validada)

```html
@if($vetAdmission->vetTests->where('is_validated', true)->count() > 0)
    <div class="flex gap-2">
        <a href="{{ route('vet.admissions.viewPdf', $vetAdmission) }}" target="_blank"
           class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
            Ver PDF
        </a>
        <a href="{{ route('vet.admissions.downloadPdf', $vetAdmission) }}"
           class="px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700">
            Descargar PDF
        </a>
        <button onclick="document.getElementById('email-modal').classList.remove('hidden')"
                class="px-4 py-2 bg-amber-600 text-white rounded-lg hover:bg-amber-700">
            Enviar por Email
        </button>
    </div>
@endif
```

### 6.2 — Modal de envío de email

Modal con campos:
- **Email** (input, pre-llenar con el email de la veterinaria o del veterinario derivante)
- **Mensaje personalizado** (textarea, opcional)
- Botón enviar → POST a `vet.admissions.sendEmail`

El dropdown o sugerencia de emails puede incluir:
- Email de la veterinaria (`$vetAdmission->customer->email`)
- Email del veterinario derivante (`$vetAdmission->veterinarian?->email`)
- Input libre para otro email

### 6.3 — Botones con Alpine.js

Implementar el modal con Alpine.js `x-show` para toggle, consistente con el resto del proyecto.

**Verificación:** Los botones aparecen solo cuando hay determinaciones validadas.
El modal de email funciona y envía correctamente.

---

## PASO 7 — Commit y tag

```bash
VERSION="v1.25.0"
SLUG="pdf-email-veterinario"

git add -A
git commit --no-verify -m "feat(vet): PDF de resultados veterinarios + envío por email

- Template PDF adaptado al contexto veterinario (animal, especie, dueño)
- Métodos downloadPdf, viewPdf, sendEmail en VetAdmissionController
- Mailable VetAdmissionResultMail con PDF adjunto
- Nombre de archivo descriptivo: LabVeterinario-Animal-Especie-Fecha.Protocolo.pdf
- Botones PDF y email en vista show (solo con determinaciones validadas)
- Modal de email con sugerencia de destinatarios"

git tag -a "${VERSION}" -m "Release ${VERSION}: PDF y email para protocolos veterinarios"
```

---

## Verificación final

- [ ] PDF se genera correctamente con datos del animal, especie, dueño
- [ ] Tabla de resultados muestra determinaciones validadas con valores de referencia por especie
- [ ] Firma del validador aparece en el PDF (si tiene firma cargada)
- [ ] Nombre del archivo es descriptivo y sin caracteres especiales
- [ ] Ver PDF abre en nueva pestaña (stream)
- [ ] Descargar PDF descarga el archivo
- [ ] Email se envía correctamente con PDF adjunto
- [ ] Asunto del email incluye nombre del animal y especie
- [ ] Modal de email pre-llena el email de la veterinaria o del vet derivante
- [ ] Botones de PDF/email solo visibles con al menos 1 determinación validada
- [ ] Lint pasa sin errores

---

## Notas / Consideraciones

- El PDF usa mPDF (misma librería que el clínico). La configuración de márgenes puede necesitar ajuste según el header veterinario.
- La configuración de correo se toma de `LabSetting` (tabla `lab_settings`), que ya tiene claves como `results_email`, `results_from_name`, `results_signature`. Son compartidas con clínico y aguas.
- Si el animal tiene nombre con caracteres especiales (ej: "Café"), `iconv` los transliteará para el nombre del archivo.
- El PDF solo muestra determinaciones con `is_validated = true`, igual que el clínico.
- La jerarquía padre/hijo en la tabla del PDF sigue el mismo patrón del clínico: padre en bold, hijos indentados con `&nbsp;`.
