# v1.26.0 — Búsqueda de protocolos veterinarios por dueño/animal

**VERSIÓN:** v1.26.0  
**SLUG:** busqueda-vet-duenio-animal  
**DEPENDENCIAS:** v1.24.0  
**ESTIMACIÓN:** 1h  
**PRIORIDAD:** media

---

## Descripción

Agrega búsqueda avanzada en el listado de protocolos veterinarios para encontrar rápidamente
protocolos por nombre del dueño, nombre del animal, o combinación de ambos. Incluye una vista
de "historial del animal" que muestra todos los protocolos de un mismo animal/dueño agrupados.

Esto es clave para el uso diario: cuando un dueño llama a pedir resultados, el recepcionista
busca por nombre y ve todos los protocolos del animal.

---

## PASO 0 — Contexto previo a leer

> ⚠️ Si venís del AGENTE_WORKFLOW.md, saltear este paso — ya lo hiciste.

Leer sin ejecutar nada:
- `app/Http/Controllers/VetAdmissionController.php` — controller con `index()` existente (v1.24.0)
- `resources/views/vet/admissions/index.blade.php` — vista del listado (v1.24.0)
- `app/Models/VetAdmission.php` — modelo con relaciones

---

## PASO 1 — Mejorar búsqueda en `VetAdmissionController::index()`

Actualmente (v1.24.0) el index tiene filtro `search` básico. Mejorar con búsqueda más completa:

```php
public function index(Request $request)
{
    $query = VetAdmission::with(['customer', 'veterinarian', 'species', 'vetTests'])
        ->orderBy('date', 'desc')
        ->orderBy('id', 'desc');

    // Búsqueda general: protocolo, dueño, animal, veterinaria
    if ($request->filled('search')) {
        $search = $request->search;
        $query->where(function ($q) use ($search) {
            $q->where('protocol_number', 'like', "%{$search}%")
              ->orWhere('animal_name', 'like', "%{$search}%")
              ->orWhere('owner_name', 'like', "%{$search}%")
              ->orWhere('breed', 'like', "%{$search}%")
              ->orWhereHas('customer', function ($q2) use ($search) {
                  $q2->where('name', 'like', "%{$search}%");
              })
              ->orWhereHas('veterinarian', function ($q2) use ($search) {
                  $q2->where('name', 'like', "%{$search}%");
              });
        });
    }

    // Filtro por especie
    if ($request->filled('species_id')) {
        $query->where('species_id', $request->species_id);
    }

    // Filtro por veterinaria
    if ($request->filled('customer_id')) {
        $query->where('customer_id', $request->customer_id);
    }

    // Filtro por rango de fechas
    if ($request->filled('date_from')) {
        $query->whereDate('date', '>=', $request->date_from);
    }
    if ($request->filled('date_to')) {
        $query->whereDate('date', '<=', $request->date_to);
    }

    // Filtro por dueño (específico, para historial)
    if ($request->filled('owner')) {
        $query->where('owner_name', 'like', '%' . $request->owner . '%');
    }

    // Filtro por animal (específico, para historial)
    if ($request->filled('animal')) {
        $query->where('animal_name', 'like', '%' . $request->animal . '%');
    }

    $admissions = $query->paginate(20)->withQueryString();
    $species = Species::where('is_active', true)->orderBy('name')->get();
    $customers = Customer::whereJsonContains('type', 'veterinario')->orderBy('name')->get();

    return view('vet.admissions.index', compact('admissions', 'species', 'customers'));
}
```

**Verificación:** Los filtros funcionan individual y combinadamente.

---

## PASO 2 — Mejorar vista index con filtros avanzados

En `resources/views/vet/admissions/index.blade.php`, actualizar la sección de filtros:

### 2.1 — Barra de búsqueda rápida

Input principal que busca en protocolo, dueño, animal, veterinaria, raza (ya existente, solo mejorar placeholder):

```html
<input type="text" name="search" value="{{ request('search') }}"
       placeholder="Buscar por protocolo, dueño, animal, raza o veterinaria..."
       class="w-full rounded-lg border-gray-300 focus:border-teal-500 focus:ring-teal-500">
```

### 2.2 — Filtros colapsables (Alpine.js)

Agregar sección de filtros avanzados con toggle:

```html
<div x-data="{ showFilters: {{ request()->hasAny(['species_id', 'customer_id', 'date_from', 'date_to', 'owner', 'animal']) ? 'true' : 'false' }} }">
    <button @click="showFilters = !showFilters" type="button"
            class="text-sm text-teal-600 hover:text-teal-800 mb-2">
        <span x-text="showFilters ? 'Ocultar filtros' : 'Filtros avanzados'"></span>
    </button>

    <div x-show="showFilters" x-transition class="grid grid-cols-1 md:grid-cols-3 gap-3 mt-2">
        <!-- Especie -->
        <select name="species_id" class="rounded-lg border-gray-300 text-sm">
            <option value="">Todas las especies</option>
            @foreach($species as $sp)
                <option value="{{ $sp->id }}" {{ request('species_id') == $sp->id ? 'selected' : '' }}>
                    {{ $sp->name }}
                </option>
            @endforeach
        </select>

        <!-- Veterinaria -->
        <select name="customer_id" class="rounded-lg border-gray-300 text-sm">
            <option value="">Todas las veterinarias</option>
            @foreach($customers as $customer)
                <option value="{{ $customer->id }}" {{ request('customer_id') == $customer->id ? 'selected' : '' }}>
                    {{ $customer->name }}
                </option>
            @endforeach
        </select>

        <!-- Dueño específico -->
        <input type="text" name="owner" value="{{ request('owner') }}"
               placeholder="Filtrar por dueño..."
               class="rounded-lg border-gray-300 text-sm">

        <!-- Animal específico -->
        <input type="text" name="animal" value="{{ request('animal') }}"
               placeholder="Filtrar por animal..."
               class="rounded-lg border-gray-300 text-sm">

        <!-- Fecha desde -->
        <input type="date" name="date_from" value="{{ request('date_from') }}"
               class="rounded-lg border-gray-300 text-sm">

        <!-- Fecha hasta -->
        <input type="date" name="date_to" value="{{ request('date_to') }}"
               class="rounded-lg border-gray-300 text-sm">
    </div>
</div>
```

**Verificación:** Filtros avanzados se expanden/colapsan. Cada filtro funciona correctamente.

---

## PASO 3 — Links de historial en la tabla de resultados

En la tabla del listado, hacer que el nombre del dueño y el nombre del animal sean **links
que filtran automáticamente** para ver el historial:

```html
<td class="px-6 py-4 whitespace-nowrap text-sm">
    <a href="{{ route('vet.admissions.index', ['animal' => $admission->animal_name, 'owner' => $admission->owner_name]) }}"
       class="text-teal-600 hover:text-teal-800 font-medium" title="Ver historial de este animal">
        {{ $admission->animal_name }}
    </a>
</td>
<td class="px-6 py-4 whitespace-nowrap text-sm">
    <a href="{{ route('vet.admissions.index', ['owner' => $admission->owner_name]) }}"
       class="text-gray-700 hover:text-teal-600" title="Ver todos los animales de este dueño">
        {{ $admission->owner_name }}
    </a>
</td>
```

Esto permite:
- Click en "Firulais" → ver todos los protocolos de Firulais del mismo dueño
- Click en "Juan Pérez" → ver todos los protocolos de todos los animales de Juan Pérez

**Verificación:** Click en nombre del animal filtra por animal+dueño. Click en dueño filtra por dueño.

---

## PASO 4 — Badge de cantidad de protocolos

En la tabla, agregar un indicador visual cuando un animal/dueño tiene múltiples protocolos:

Opción: al hacer la query, contar protocolos por `owner_name + animal_name` y mostrar
un badge "×3" si tiene más de 1 protocolo. Esto es opcional y se puede implementar con
una subquery o un atributo calculado.

Alternativa más simple: no agregar el badge ahora y dejar que el link de historial
sea suficiente. El usuario hace click y ve cuántos hay.

**Verificación:** (Si se implementa) Badge visible para animales con múltiples protocolos.

---

## PASO 5 — Indicadores en vista show

En `resources/views/vet/admissions/show.blade.php`, agregar en la sección de datos del animal:

```html
@php
    $historyCount = \App\Models\VetAdmission::where('owner_name', $vetAdmission->owner_name)
        ->where('animal_name', $vetAdmission->animal_name)
        ->count();
@endphp

@if($historyCount > 1)
    <a href="{{ route('vet.admissions.index', ['animal' => $vetAdmission->animal_name, 'owner' => $vetAdmission->owner_name]) }}"
       class="inline-flex items-center text-sm text-amber-600 hover:text-amber-800">
        Ver historial ({{ $historyCount }} protocolos)
    </a>
@endif
```

**Verificación:** En el detalle de un protocolo, si el animal tiene más de 1 protocolo, se muestra el link de historial.

---

## PASO 6 — Commit y tag

```bash
VERSION="v1.26.0"
SLUG="busqueda-vet-duenio-animal"

git add -A
git commit --no-verify -m "feat(vet): búsqueda avanzada de protocolos por dueño y animal

- Búsqueda en protocolo, dueño, animal, raza, veterinaria, veterinario
- Filtros avanzados colapsables: especie, veterinaria, dueño, animal, fechas
- Links de historial en nombres de animal y dueño
- Link de historial en vista show cuando hay múltiples protocolos
- Filtros combinables con query string persistente"

git tag -a "${VERSION}" -m "Release ${VERSION}: Búsqueda de protocolos veterinarios por dueño/animal"
```

---

## Verificación final

- [ ] Búsqueda general encuentra por protocolo, dueño, animal, raza, veterinaria
- [ ] Filtro por especie funciona
- [ ] Filtro por veterinaria funciona
- [ ] Filtro por rango de fechas funciona
- [ ] Filtro por dueño específico funciona
- [ ] Filtro por animal específico funciona
- [ ] Filtros combinados funcionan (ej: especie + dueño)
- [ ] Click en nombre del animal filtra por animal + dueño
- [ ] Click en nombre del dueño filtra por dueño
- [ ] Link de historial en vista show funciona
- [ ] Filtros avanzados se colapsan/expanden con Alpine.js
- [ ] Paginación conserva los filtros (withQueryString)
- [ ] Lint pasa sin errores

---

## Notas / Consideraciones

- La búsqueda por dueño/animal es por `LIKE`, no es exacta. Esto permite encontrar "Juan" cuando el dueño se llama "Juan Pérez".
- Los links de historial usan el nombre exacto del dueño/animal. Si hay variaciones de escritura (ej: "Juan Perez" vs "Juan Pérez"), no se agruparán. Esto es aceptable para la primera versión.
- A futuro se podría crear una tabla `animals` o `pets` para normalizar los datos del animal y vincular protocolos al mismo registro, evitando duplicación de datos. Pero eso es una feature mayor que excede el scope actual.
- El filtro colapsable arranca abierto si ya hay filtros aplicados, para que el usuario vea qué filtros tiene activos.
