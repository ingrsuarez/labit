# v1.63.1 — Percepciones en NC de proveedor (espejo de v1.63.0)

**VERSIÓN:** v1.63.1
**SLUG:** percepciones-nc-proveedor
**DEPENDENCIAS:** v1.63.0 (catálogo `PurchasePerception` + patrón pivote en FC)
**ESTIMACIÓN:** 2-3 horas
**PRIORIDAD:** media — completa la simetría contable iniciada en v1.63.0

---

## Descripción

Replicar en Notas de Crédito de proveedor el módulo de percepciones implementado en v1.63.0
para Facturas de Compra. Cuando llega una NC con percepciones, se cargan vía pivote
`purchase_credit_note_perceptions` y el asiento contable acredita cada cuenta de anticipo
(revertir el débito que hizo la FC original).

**Reusa el catálogo `PurchasePerception` existente** — no se crea catálogo nuevo.

---

## PASO 0 — Contexto previo

> Si venís del AGENTE_WORKFLOW.md, saltear este paso.

**Documentación de la versión madre:**
- `agent-bootstrap/handoffs/v1.63.0-pm-to-dev.md` — diagnóstico completo
- `agent-bootstrap/prompts/completados/v1.63.0-percepciones-compra.md` — referencia técnica del patrón
- `docs/design/v1.63.0-percepciones-compra.md` — el bloque visual de "Percepciones" en NC sigue el mismo patrón que en FC

**Documentación del proyecto:**
- `BLUEPRINT.md` — DD-009 (de v1.63.0)
- `STATUS.md`

**Código de referencia (v1.63.0 ya implementada):**
- `app/Models/PurchaseInvoicePerception.php` — modelo análogo
- `database/migrations/<ts>_create_purchase_invoice_perceptions_table.php` — esquema análogo
- `resources/views/purchase-invoices/create.blade.php` — bloque de percepciones (sección Alpine de líneas)
- `app/Http/Controllers/PurchaseInvoiceController.php` — store/update con procesamiento de líneas

**Código a modificar:**
- `app/Models/PurchaseCreditNote.php` — agregar relación + ajustar `recalculate()`
- `app/Http/Controllers/PurchaseCreditNoteController.php` — store/update
- `app/Services/AccountingEntryService.php` — método `fromPurchaseCreditNote()`
- `resources/views/purchase-credit-notes/create.blade.php` — agregar bloque Alpine

---

## PASO 1 — Migración y modelo `PurchaseCreditNotePerception`

Crear migración:

```bash
php artisan make:migration create_purchase_credit_note_perceptions_table --create=purchase_credit_note_perceptions
```

Esquema (idéntico al de FC pero con FK al padre NC):

```php
Schema::create('purchase_credit_note_perceptions', function (Blueprint $table) {
    $table->id();
    $table->foreignId('purchase_credit_note_id')->constrained()->cascadeOnDelete();
    $table->foreignId('purchase_perception_id')
          ->nullable()
          ->constrained()
          ->nullOnDelete();
    $table->foreignId('accounting_account_id')
          ->constrained('accounting_accounts')
          ->restrictOnDelete();
    $table->string('name_snapshot');
    $table->string('jurisdiction_snapshot')->nullable();
    $table->decimal('rate_snapshot', 5, 2)->default(0);
    $table->decimal('amount', 14, 2);
    $table->unsignedSmallInteger('sort_order')->default(0);
    $table->timestamps();

    $table->index('purchase_credit_note_id');
});
```

Crear modelo `app/Models/PurchaseCreditNotePerception.php` (espejo de `PurchaseInvoicePerception`):

- `$fillable`: `purchase_credit_note_id`, `purchase_perception_id`, `accounting_account_id`, `name_snapshot`, `jurisdiction_snapshot`, `rate_snapshot`, `amount`, `sort_order`
- `$casts`: `amount => 'decimal:2'`, `rate_snapshot => 'decimal:2'`, `sort_order => 'integer'`
- Relaciones: `purchaseCreditNote()`, `perception()` → `PurchasePerception`, `accountingAccount()`

En `app/Models/PurchaseCreditNote.php`, agregar:

```php
public function perceptions(): HasMany
{
    return $this->hasMany(PurchaseCreditNotePerception::class)->orderBy('sort_order');
}
```

Y modificar `recalculate()`:

```php
public function recalculate(): void
{
    $this->subtotal = round((float) $this->items()->reorder()->sum(DB::raw('quantity * unit_price')), 2);
    $ivaByRate = $this->items()->reorder()
        ->selectRaw('iva_rate, SUM(iva_amount) as total_iva')
        ->groupBy('iva_rate')
        ->pluck('total_iva', 'iva_rate');
    $this->iva_21 = $ivaByRate[21.00] ?? $ivaByRate['21.00'] ?? 0;
    $this->iva_10_5 = $ivaByRate[10.50] ?? $ivaByRate['10.50'] ?? 0;
    $this->iva_27 = $ivaByRate[27.00] ?? $ivaByRate['27.00'] ?? 0;

    // NEW: percepciones desde la pivote
    $this->percepciones = round((float) $this->perceptions()->sum('amount'), 2);

    $this->total = round(
        (float) $this->subtotal + (float) $this->iva_21 + (float) $this->iva_10_5 + (float) $this->iva_27
        + (float) $this->percepciones + (float) $this->otros_impuestos,
        2
    );
    $this->save();
}
```

**Verificación:**
- `php artisan migrate` corre sin error
- En tinker: crear NC, asociar pivote, leer `->perceptions`

---

## PASO 2 — UI: Bloque "Percepciones" en NC

Modificar `resources/views/purchase-credit-notes/create.blade.php` (y `edit.blade.php` si existe):

Replicar el mismo patrón Alpine que ya existe en `purchase-invoices/create.blade.php`
(después de v1.63.0):

- Pasar `$perceptionTypes` y `$perceptionLines` desde el controller (mismo formato que en FC)
- Tabla con líneas: selector de tipo + alícuota (texto solo lectura) + monto manual + eliminar
- Botón "Agregar percepción"
- Suma `perceptionsTotal` que se incluye en el total de la NC
- Inputs `name="perceptions[i][...]"` con los mismos campos que en FC (`purchase_perception_id`, `name_snapshot`, `jurisdiction_snapshot`, `rate_snapshot`, `accounting_account_id`, `amount`)

**No hace falta diseño nuevo** — usar exactamente el mismo HTML/Alpine que en FC. Copiar
el bloque y ajustar la ubicación si la maquetación de NC es distinta.

**Verificación:**
- En `create` de NC: agregar percepciones, completar montos, guardar, ver pivote.
- En `edit` (si existe): editar y persistir.

---

## PASO 3 — `PurchaseCreditNoteController` y procesamiento de líneas

### En `create()` y `edit()`

Pasar a la vista:

```php
$perceptionTypes = PurchasePerception::query()
    ->where('company_id', active_company_id())
    ->active()
    ->orderBy('sort_order')
    ->orderBy('name')
    ->get();

$perceptionLines = $purchaseCreditNote?->perceptions->map(fn ($p) => [
    'purchase_perception_id' => $p->purchase_perception_id,
    'name_snapshot' => $p->name_snapshot,
    'jurisdiction_snapshot' => $p->jurisdiction_snapshot,
    'rate_snapshot' => $p->rate_snapshot,
    'accounting_account_id' => $p->accounting_account_id,
    'amount' => $p->amount,
])->toArray() ?? [];
```

### En `store()` y `update()`

Después de validar y guardar la NC + items, procesar percepciones:

```php
$creditNote->perceptions()->delete();
foreach ($request->input('perceptions', []) as $idx => $line) {
    if (!empty($line['accounting_account_id']) && (float) ($line['amount'] ?? 0) > 0) {
        $creditNote->perceptions()->create([
            'purchase_perception_id' => $line['purchase_perception_id'] ?: null,
            'accounting_account_id' => $line['accounting_account_id'],
            'name_snapshot' => $line['name_snapshot'] ?? '',
            'jurisdiction_snapshot' => $line['jurisdiction_snapshot'] ?? null,
            'rate_snapshot' => $line['rate_snapshot'] ?? 0,
            'amount' => $line['amount'],
            'sort_order' => $idx,
        ]);
    }
}

$creditNote->recalculate();
```

Validación:
- `perceptions.*.accounting_account_id`: `nullable|exists:accounting_accounts,id`
- `perceptions.*.amount`: `nullable|numeric|min:0`

**Verificación:**
- Crear NC con 2 percepciones → pivote tiene 2 filas, NC.percepciones cache correcto.
- Editar NC y cambiar percepciones → cambio reflejado.

---

## PASO 4 — `AccountingEntryService::fromPurchaseCreditNote()` corregido

Modificar el método para que acredite cada percepción en su cuenta snapshot (revierte
el débito que hizo la FC):

```php
public function fromPurchaseCreditNote(PurchaseCreditNote $creditNote): ?JournalEntry
{
    $creditNote->loadMissing(['items', 'supplier', 'perceptions.accountingAccount']);

    $itemsWithSupply = $creditNote->items->whereNotNull('supply_id');
    $itemsWithoutSupply = $creditNote->items->whereNull('supply_id');

    $netoInsumos = round($itemsWithSupply->sum(fn ($i) => (float) $i->quantity * (float) $i->unit_price), 2);
    $netoServicios = round($itemsWithoutSupply->sum(fn ($i) => (float) $i->quantity * (float) $i->unit_price), 2);

    $totalIva = round(
        (float) $creditNote->iva_21 + (float) $creditNote->iva_10_5 + (float) $creditNote->iva_27,
        2
    );
    $totalBruto = round((float) $creditNote->total, 2);

    // NEW: otros_impuestos sigue como gasto reverso; percepciones van a sus cuentas
    $otrosImp = round((float) $creditNote->otros_impuestos, 2);

    $lines = [];

    // DB Proveedor (reduce deuda) por el total bruto
    $lines[] = [
        'account_code' => '2.1.01',
        'debit' => $totalBruto,
        'credit' => 0,
        'description' => 'Reduce deuda '.($creditNote->supplier->name ?? '').' — NC '.$creditNote->full_number,
    ];

    $netoServiciosPlus = $netoServicios + $otrosImp;  // sin percepciones

    if ($netoInsumos > 0) {
        $lines[] = [
            'account_code' => '5.1.01',
            'debit' => 0,
            'credit' => $netoInsumos,
            'description' => 'Reversión insumos — NC compra '.$creditNote->full_number,
        ];
    }

    if ($netoServiciosPlus > 0) {
        $lines[] = [
            'account_code' => '5.1.02',
            'debit' => 0,
            'credit' => $netoServiciosPlus,
            'description' => 'Reversión servicios / otros — NC compra '.$creditNote->full_number,
        ];
    }

    if ($totalIva > 0) {
        $lines[] = [
            'account_code' => '1.1.06',
            'debit' => 0,
            'credit' => $totalIva,
            'description' => 'IVA crédito fiscal NC proveedor',
        ];
    }

    // NEW: una línea CR por cada percepción, en su cuenta snapshot (revierte el anticipo)
    foreach ($creditNote->perceptions as $perception) {
        $amt = round((float) $perception->amount, 2);
        if ($amt <= 0 || ! $perception->accountingAccount) {
            continue;
        }
        $lines[] = [
            'account_code' => $perception->accountingAccount->code,
            'debit' => 0,
            'credit' => $amt,
            'description' => 'Reversión percepción '.$perception->name_snapshot
                .($perception->jurisdiction_snapshot ? ' ('.$perception->jurisdiction_snapshot.')' : '')
                .' — NC '.$creditNote->full_number,
        ];
    }

    return $this->createEntry(
        (int) $creditNote->company_id,
        Carbon::parse($creditNote->issue_date),
        'NC proveedor — '.$creditNote->full_number,
        $creditNote,
        $lines
    );
}
```

**Verificación:**
- Crear NC con 1 percepción IVA y 1 percepción IIBB Neuquén
- Asiento generado: cada percepción CR en su cuenta snapshot
- Total débito = total crédito (balanceado)

---

## PASO 5 — Pantalla de saldos (ajuste menor)

La pantalla `/purchase-perceptions/balances` ya creada en v1.63.0 usa `PurchaseInvoicePerception`
para los anticipos cargados. Ahora también debe sumar `PurchaseCreditNotePerception` (con
signo NEGATIVO porque las NC reducen el anticipo):

En `app/Services/PurchasePerceptionBalanceService.php`, ajustar el cálculo de `anticipos`:

```php
$anticiposFc = PurchaseInvoicePerception::query()
    ->where('purchase_perception_id', $perception->id)
    ->whereHas('purchaseInvoice', function ($q) use ($from, $to) {
        $q->whereBetween('issue_date', [$from, $to]);
    })
    ->sum('amount');

$anticiposNc = PurchaseCreditNotePerception::query()
    ->where('purchase_perception_id', $perception->id)
    ->whereHas('purchaseCreditNote', function ($q) use ($from, $to) {
        $q->whereBetween('issue_date', [$from, $to]);
    })
    ->sum('amount');

$anticipos = $anticiposFc - $anticiposNc;  // NC reducen el anticipo
```

(El "Saldo en cuenta" que viene del Libro Mayor ya incluye automáticamente los créditos
por NC porque lee `journal_entry_lines` directamente.)

**Verificación:**
- Cargar 1 FC con percepción de $1000 y 1 NC con percepción de $200 → "Anticipos cargados" muestra $800.
- "Saldo en cuenta" muestra $800 también (asiento FC: DB 1000; asiento NC: CR 200; saldo neto: 800).

---

## PASO 6 — Tests Feature

Crear `tests/Feature/PurchasePerception/PurchaseCreditNotePerceptionTest.php`:

- `test_nc_de_proveedor_con_percepciones_se_guarda_correctamente()`
- `test_recalculate_de_nc_suma_percepciones_desde_pivote()`
- `test_asiento_contable_acredita_cada_percepcion_en_su_cuenta_snapshot()`
- `test_eliminar_nc_borra_lineas_de_pivote()`
- `test_balance_resta_percepciones_de_nc()`

**Verificación:**
```bash
php artisan test --filter=PurchaseCreditNotePerception
```
Verde.

---

## PASO 7 — Documentación

Actualizar `BLUEPRINT.md` DD-009 (extendido en v1.63.1):

Modificar el párrafo de DD-009 para mencionar que el patrón de pivote también aplica a
NC de proveedor (`PurchaseCreditNotePerception`) y que el asiento de NC acredita en lugar
de debitar.

---

## PASO 8 — Commit y tag

```bash
VERSION="v1.63.1"
SLUG="percepciones-nc-proveedor"

git add app/Models/PurchaseCreditNote.php \
        app/Models/PurchaseCreditNotePerception.php \
        app/Http/Controllers/PurchaseCreditNoteController.php \
        app/Services/AccountingEntryService.php \
        app/Services/PurchasePerceptionBalanceService.php \
        database/migrations/*purchase_credit_note_perceptions* \
        resources/views/purchase-credit-notes/ \
        tests/Feature/PurchasePerception/PurchaseCreditNotePerceptionTest.php \
        BLUEPRINT.md

git commit --no-verify -m "feat(compras): percepciones en NC de proveedor (espejo de v1.63.0)

- Modelo PurchaseCreditNotePerception (pivote con snapshots)
- Bloque Alpine en formulario de NC (mismo patrón que FC)
- AccountingEntryService::fromPurchaseCreditNote acredita cada percepción
  en su cuenta snapshot (revierte el anticipo)
- PurchaseCreditNote::recalculate suma percepciones desde la pivote
- PurchasePerceptionBalanceService resta percepciones de NC del saldo de anticipos
- Tests Feature de NC con percepciones
- DD-009 extendido en BLUEPRINT"

git tag -a "${VERSION}" -m "Release ${VERSION}: percepciones en NC de proveedor"
```

---

## Verificación final

- [ ] `php artisan migrate` corre sin error
- [ ] `php artisan test --filter=PurchaseCreditNotePerception` → todos verde
- [ ] NC create muestra el bloque "Percepciones" con multi-línea (mismo patrón visual que FC)
- [ ] `purchase_credit_notes.percepciones` (cache) se sincroniza con la pivote
- [ ] Asiento de NC: cada percepción CR en su cuenta snapshot, no en `5.1.02`
- [ ] Pantalla de saldos resta las percepciones de NC del total de anticipos
- [ ] BLUEPRINT.md actualizado con DD-009 extendido

---

## Notas

- **No requiere Designer** — el bloque visual es el mismo que se diseñó en v1.63.0.
- **No migrar retroactivamente NC viejas** — mantienen el asiento original.
- **Reusa el catálogo `PurchasePerception`** — no se crea un catálogo paralelo.
- Si v1.64.0 (DDJJ + imputaciones) se hace después y agrega columna `tax_id` a `purchase_perceptions`,
  esta versión NO requiere cambios.
