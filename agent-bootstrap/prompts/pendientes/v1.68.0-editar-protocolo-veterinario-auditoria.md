# v1.68.0 — Editar protocolo veterinario con auditoría

**Tipo:** minor — nueva funcionalidad
**Prioridad:** alta — requerimiento operativo en producción
**Dependencias:** ninguna (infra Auditable ya existe desde v2.5.0)

---

## Qué hace

Agrega un formulario de edición a los protocolos veterinarios (`VetAdmission`).
Permite modificar los datos de cabecera (animal, dueño, veterinaria, especie, fecha, etc.).
Cada edición queda registrada en `audit_logs` con el detalle de qué campos cambiaron,
los valores anteriores y nuevos, y quién realizó el cambio.

Solo pueden editar los roles `admin` y `bioquimico`.

---

## Contexto — Archivos a leer antes de empezar

```
app/Http/Controllers/VetAdmissionController.php   ← controller existente (sin edit/update)
app/Models/VetAdmission.php                        ← fillable, trait Auditable, relaciones
app/Traits/Auditable.php                           ← logAudit(action, description)
database/seeders/RolesAndPermissionsSeeder.php     ← permisos existentes, roles admin/bioquimico
routes/web.php                                     ← bloque Route::prefix('vet') líneas ~255-276
resources/views/vet/admissions/create.blade.php    ← formulario de alta (base para el edit)
resources/views/vet/admissions/show.blade.php      ← vista show (para agregar botón Editar)
```

---

## PASO 1 — Agregar permiso `vet-admissions.edit` al seeder

En `database/seeders/RolesAndPermissionsSeeder.php`:

1. Agregar `'vet-admissions.edit'` al array `$permissions` general (sección de permisos de lab).
2. Agregar `'vet-admissions.edit'` al array de permisos del rol `admin` (ya tiene todos los permisos, pero debe estar en la lista base).
3. Agregar `'vet-admissions.edit'` al array `$bioquimicoPermissions`.

Ejecutar el seeder para aplicar:
```bash
php artisan db:seed --class=RolesAndPermissionsSeeder
```

---

## PASO 2 — Agregar rutas `edit` y `update`

En `routes/web.php`, dentro del bloque `Route::prefix('vet')`, agregar después de la ruta `show`:

```php
Route::get('admissions/{vetAdmission}/edit', [App\Http\Controllers\VetAdmissionController::class, 'edit'])->name('vet.admissions.edit');
Route::put('admissions/{vetAdmission}', [App\Http\Controllers\VetAdmissionController::class, 'update'])->name('vet.admissions.update');
```

---

## PASO 3 — Implementar `edit()` y `update()` en el controller

En `VetAdmissionController`:

### Método `edit()`

```php
public function edit(VetAdmission $vetAdmission)
{
    $this->authorize('vet-admissions.edit');

    $vetAdmission->load(['customer', 'veterinarian', 'species', 'labBranch']);

    $customers = Customer::whereJsonContains('type', 'veterinario')->orderBy('name')->get();
    $species = Species::where('is_active', true)->orderBy('name')->get();
    $branches = \App\Models\LabBranch::active()->orderByDesc('is_central')->orderBy('name')->get();

    // Veterinarios del cliente actual para precargar el dropdown
    $veterinarians = $vetAdmission->customer
        ? $vetAdmission->customer->veterinarians()->where('is_active', true)->get(['id', 'name', 'matricula'])
        : collect();

    return view('vet.admissions.edit', compact('vetAdmission', 'customers', 'species', 'branches', 'veterinarians'));
}
```

### Método `update()`

Comparar campos antes de actualizar y registrar en auditoría solo lo que cambió.

```php
public function update(Request $request, VetAdmission $vetAdmission)
{
    $this->authorize('vet-admissions.edit');

    $request->validate([
        'customer_id'      => 'required|exists:customers,id',
        'veterinarian_id'  => 'nullable|exists:veterinarians,id',
        'species_id'       => 'required|exists:species,id',
        'animal_name'      => 'required|string|max:255',
        'owner_name'       => 'required|string|max:255',
        'owner_phone'      => 'nullable|string|max:50',
        'owner_email'      => 'nullable|email|max:255',
        'breed'            => 'nullable|string|max:100',
        'age'              => 'nullable|string|max:50',
        'date'             => 'required|date',
        'observations'     => 'nullable|string',
        'lab_branch_id'    => 'nullable|exists:lab_branches,id',
    ]);

    // Campos auditables con etiquetas legibles
    $auditableFields = [
        'customer_id'     => ['label' => 'Veterinaria',   'resolve' => fn($id) => \App\Models\Customer::find($id)?->name ?? $id],
        'veterinarian_id' => ['label' => 'Veterinario',   'resolve' => fn($id) => $id ? (\App\Models\Veterinarian::find($id)?->name ?? $id) : 'Sin derivante'],
        'species_id'      => ['label' => 'Especie',       'resolve' => fn($id) => \App\Models\Species::find($id)?->name ?? $id],
        'animal_name'     => ['label' => 'Animal',        'resolve' => fn($v) => $v],
        'owner_name'      => ['label' => 'Dueño',         'resolve' => fn($v) => $v],
        'owner_phone'     => ['label' => 'Tel. Dueño',    'resolve' => fn($v) => $v ?? '—'],
        'owner_email'     => ['label' => 'Email Dueño',   'resolve' => fn($v) => $v ?? '—'],
        'breed'           => ['label' => 'Raza',          'resolve' => fn($v) => $v ?? '—'],
        'age'             => ['label' => 'Edad',          'resolve' => fn($v) => $v ?? '—'],
        'date'            => ['label' => 'Fecha',         'resolve' => fn($v) => \Carbon\Carbon::parse($v)->format('d/m/Y')],
        'observations'    => ['label' => 'Observaciones', 'resolve' => fn($v) => $v ?? '—'],
        'lab_branch_id'   => ['label' => 'Sede',          'resolve' => fn($id) => $id ? (\App\Models\LabBranch::find($id)?->name ?? $id) : '—'],
    ];

    $changes = [];
    foreach ($auditableFields as $field => $config) {
        $oldRaw = $vetAdmission->$field;
        $newRaw = $request->input($field);

        // Normalizar nulls y strings vacíos para comparación justa
        $oldNorm = $oldRaw === null ? '' : (string) $oldRaw;
        $newNorm = $newRaw === null ? '' : (string) $newRaw;

        if ($oldNorm !== $newNorm) {
            $resolve = $config['resolve'];
            $changes[] = $config['label'].': '.$resolve($oldRaw).' → '.$resolve($newRaw);
        }
    }

    $vetAdmission->update([
        'customer_id'     => $request->customer_id,
        'veterinarian_id' => $request->veterinarian_id ?: null,
        'species_id'      => $request->species_id,
        'animal_name'     => $request->animal_name,
        'owner_name'      => $request->owner_name,
        'owner_phone'     => $request->owner_phone,
        'owner_email'     => $request->owner_email,
        'breed'           => $request->breed,
        'age'             => $request->age,
        'date'            => $request->date,
        'observations'    => $request->observations,
        'lab_branch_id'   => $request->lab_branch_id,
    ]);

    if (!empty($changes)) {
        $vetAdmission->logAudit('updated', 'Protocolo editado. Cambios: '.implode('. ', $changes));
    }

    return redirect()->route('vet.admissions.show', $vetAdmission)
        ->with('success', 'Protocolo actualizado correctamente.');
}
```

---

## PASO 4 — Crear vista `edit.blade.php`

Crear `resources/views/vet/admissions/edit.blade.php`.

Tomar como base `create.blade.php` y adaptarlo:

- Título: `Editar Protocolo {{ $vetAdmission->protocol_number }}`
- El `<form>` debe usar `method="POST"` con `@method('PUT')` y `action="{{ route('vet.admissions.update', $vetAdmission) }}"`
- Precargar todos los campos con los valores actuales del protocolo (`old('field', $vetAdmission->field)`)
- El dropdown de veterinarios referentes debe precargar con `$veterinarians` y seleccionar `$vetAdmission->veterinarian_id`
- El selector de veterinaria (customer) debe tener Alpine.js para cargar veterinarios vía `vet.customer.veterinarians` al cambiar (igual que en create)
- Botón de cancelar → `route('vet.admissions.show', $vetAdmission)`
- **No incluir** el bloque de agregar determinaciones (eso se hace desde el show)

---

## PASO 5 — Agregar botón "Editar Protocolo" en el show

En `resources/views/vet/admissions/show.blade.php`, en la barra de botones de acciones (donde están Ver PDF, Descargar PDF, Enviar por Email, Etiqueta, Validar Todos, Agregar Prácticas, Facturar), agregar:

```blade
@can('vet-admissions.edit')
    <a href="{{ route('vet.admissions.edit', $vetAdmission) }}"
       class="btn btn-secondary">
        ✏️ Editar Protocolo
    </a>
@endcan
```

Ubicar el botón al inicio de la barra de acciones (antes de Ver PDF), para que sea fácil de encontrar.

---

## PASO 6 — Verificar que auditoría se muestra en la vista centralizada

La tabla `audit_logs` es polimórfica y `VetAdmission` ya usa el trait `Auditable`.
Verificar en `/auditoria` (AuditController) que los logs de `VetAdmission` aparecen correctamente
con el label del modelo (`auditable_type`). Si el controller filtra por tipo de modelo,
confirmar que `App\Models\VetAdmission` está incluido o no está excluido.

No es necesario modificar el AuditController si ya muestra todos los modelos.

---

## PASO 7 — Commit y tag

```bash
git add -A
git commit -m "feat(vet): edición de protocolo veterinario con auditoría de cambios

- Permiso vet-admissions.edit → admin + bioquimico
- VetAdmissionController: edit() + update() con authorize()
- update(): diff campo por campo → logAudit() con cambios legibles
- Vista vet/admissions/edit.blade.php (base: create, preloaded)
- Botón Editar Protocolo en show (@can vet-admissions.edit)
- Rutas GET /vet/admissions/{id}/edit y PUT /vet/admissions/{id}

Cierra requerimiento: edición de cabecera + trazabilidad completa"
git tag -a v1.68.0 -m "v1.68.0 — Editar protocolo veterinario con auditoría"
```
